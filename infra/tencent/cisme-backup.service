[Unit]
Description=CISME local PostgreSQL backup
After=postgresql.service

[Service]
Type=oneshot
User=postgres
Group=postgres
UMask=0077
ExecStart=/bin/sh -c '/usr/bin/pg_dump -Fc cisme > /var/backups/cisme/cisme-$(date -u +%%Y%%m%%dT%%H%%M%%SZ).dump'

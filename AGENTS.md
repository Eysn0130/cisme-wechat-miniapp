# CISME 交付重点

- 产品是原生微信小程序，消费者页面实现位于 `apps/miniprogram`（WXML/WXSS/TypeScript），不是网页产品，也不是独立手机 App。
- 用户于 2026-09-08 再次确认：优先完善微信小程序端的展示页面。冻结 Web 与用户明确接受的后续修订仅作为视觉参考；Web 构建通过、网页截图或网页交互不能替代小程序验收。
- 逐页检查应优先使用微信开发者工具与获授权的微信真机，核对胶囊避让、安全区、文字排版、图片、滚动、键盘、导航及真实接口状态。没有实测的状态不得写成通过。
- 每次当前源码验收需核对包哈希，保留历史截图的原始来源与范围；非视觉修改也不能把旧截图自动声明为新的全状态证据。
- 公网 API、正式账号、合法域名和团队扫码体验版均服务于小程序交付。不能用静态网页上线或临时调试二维码替代体验版。
- 用户在操作开发者工具时，不抢占其页面；继续可独立完成的源码与接口工作。
- 首页必须允许游客看到“授权身份并开始”，不得在 `onShow` 自动把游客重定向走；护理周期从 `planned` 开始须经用户显式确认。每次到期护理必须按 00 净澈、01 清洁、02 修护、03 精护顺序完成并提交护理后自我感受，服务端持久化分步事实；记录页必须可回看这些事实，并对旧记录诚实显示详细事实缺失。

## 移动端设计决策链

- 小程序 UI/UX 决策严格按以下顺序裁决：PRD 与真实业务状态 → 微信小程序原生能力和平台约束 → `mobile-ui-ux-designer` → `design-taste-frontend` → 微信开发者工具与真机验证。后层不得改写前层已经确定的事实与约束。
- `mobile-ui-ux-designer` 是移动端 UX / Interaction Design 的主 Skill，负责信息架构、页面状态、操作路径、Bottom Sheet、Tab Bar、FAB、滚动与手势、动画时序、中断恢复、可访问性及完整 interaction contract；其通用 iOS、Android 或 Web 建议必须翻译为微信小程序原生组件和行为。
- `design-taste-frontend` 仅作辅助视觉审阅，负责版式、字体、间距、层级、动效质感、反模板化与避免 AI 味；其 Web 规则与微信原生规范冲突时，以微信原生规范为准。
- 不为了套用 Skill 增加依赖、复制组件体系或重构已有正确架构。`mobile-app-ux-auditor` 不作为常驻 Skill；仅在一个完整页面或流程完成后阶段性检查 loading、empty、error、interruption、resume、navigation 与 accessibility。

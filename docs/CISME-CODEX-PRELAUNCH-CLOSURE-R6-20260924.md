# CISME R6：现有业务与原生体验收口执行指令

日期：2026-09-24（America/Los_Angeles）。全文 A—L 必须读完。执行本指令，不启动 Goal。

## A. 目标、工程与恢复锚点

继续唯一工程 `/Users/mini/CISME`，仓库 `Eysn0130/cisme-wechat-miniapp`，原分支 `codex/fulfillment-lifecycle-20260922` 与原 PR22。目标：备案通过前，现有源码、main、正确 production 部署及体验验证完成；平台条件满足后仅剩微信提交、审核和发布步骤。备案通过不等于免除微信版本审核。

先读取实时 PR22、远端分支、main、本机 HEAD 与工作树。正常 fetch/快进或逐项整合，保留本机提交和未提交内容，不 reset、不强推、不重放已在远端的补丁。ChatGPT R6 已有提交 `056b9e2dce770c383886826b4765d24fc397a96e`、`4c3ee65c819ef7ff98cc4a6fb54706b4fa881d10`，以及包含本文件的接续提交；最终精确 HEAD 和 CI 以实时 PR22 与 R6 回执为准。

读取原 AGENTS、`docs/product/CISME-产品需求文档-PRD-V2.1-R4.md`、R3—R5 已确认决策、R6 复核材料和实际生产恢复材料。聊天附件缺失时，本仓库文件可独立执行，不停在寻找旧附件。旧 SHA 的成功不能证明新 HEAD 通过。

## B. 已冻结事实与不扩张边界

主体：熹芃（上海）生物科技有限公司。唯一正式客服：现有微信小程序客服。交易及关联客服/售后按 36 个日历月基线及更高优先级的法定要求、争议保全处理；普通咨询按现有有限期限配置。其他非必要个人资料最短必要保留、删除或匿名化。逐单发送真实退货地址，不要求预设固定地址。

不建设第二后台、第二客服/售后、独立注销中心、购物车、多仓 ERP、转账或新设计体系；不换框架，不扩大商业政策。复用 WXML/WXSS/TypeScript、现有会员与管理入口。普通本人隐私请求不默认双人审批、不新增身份证/人脸材料。不能借“精简”取消已有订单、退款、余额权益、必要告知、导出删除与最小注销能力。

已确认事实不再反复索取。先查既有系统、实际数据流与批准记录；不能编造的真实第三方、合同、环境或联系人事实，只集中提出最小确认，其他工作继续。

## C. 接收 R6 修复并完成准确回归

保留以下已修实现及 21 项新增回归 `tests/unit/native-r6-request-ownership.test.ts`：

1. checkout：刷新请求按轮次归属，加载/同步失败/跳转期间阻止报价与下单，写入进行时不再交叉刷新；入口数量整数化；保留未知结果幂等键和旧报价失效反馈。
2. support：订单选择器防跨身份、A→B→A、关闭重开旧响应覆盖；文字输入不再使合法订单卡片读取失效；用户移除的订单不被晚到响应或路由参数暗中重新关联；注销后的历史订单上下文仍不可任意替换。
3. privacy-rights：取消分享不是办理失败；补充附件交给微信后由分享回调清理，不能被 onHide 提前删除；尚未交接的下载离页仍中止；过期/撤销副本不显示无效补充附件按钮。
4. 结算页删重复说明和重复库存提示；局部改善文字可读性，保留 44px 数量点击区域和 18px 图标、既有紫色品牌。不将这些源码变更冒充真机视觉通过。

历史 CI：`36009811086` 在严格类型检查失败；`36011345400` 类型通过但完整单测发现 5 项失败（两个旧下单夹具没有设置已加载且可下单，文案与动作清单旧预期、重试按钮缺禁用样式）。接续提交修复这些问题，不删除测试，不放宽加载/权限/资金守卫。最终须读取接续 HEAD 自己的 CI 结果。依赖未增加，临时源码取证 workflow 已恢复 dispatch-only，不再扩大自动源码打包。

## D. 其他隐私请求：实际办理，不只是回复

重点读 `services/api/src/privacyRights.ts`、`privacyExecution.ts`、`formalPrivacyExecution.ts`、`optionalProfileErasure.ts` 和现有 `management-privacy` 页面。

可选账户资料删除、最小注销、正式资料副本与单独附件交付已有实现，不要重建。剩余“处理其他数据”、更正和撤回请求，需要现有请求从范围确认走到真实执行与可信终态，不能回复一句完成却没有数据变化。

按现有数据域增加最小执行分支：明确本人、对象、字段/消息/素材范围、可删除与必须保留项目、理由、必要确认、并发版本、审计与执行结果。不得执行用户输入的任意表名或任意 SQL。允许部分完成，但必须列出已处理、继续保留及其理由；更正要确实写入合法字段；撤回要停止对应可选后续处理，不误删交易依据。

删除执行应使旧导出、在建导出和缓存不再重新暴露资料；沿用现有删除标记/抑制账本与版本机制。同步 `docs/privacy/subject-data-map.json`、实际个人信息清单和代码，不把过期的 disabled/policy_pending 标签当成用户没批准，也不能仅把标签全部改成 true。

验证：正式形态本人成功、更正后重读、删除后重读、部分保留、他人越权、重复请求、并发保全、后台中断恢复、导出与删除并发。使用隔离数据库、正式配置形态和受控外部接口，不需要真实用户数据。

## E. 导出：大副本、缺失对象与真实交付

重点读 `formalPrivacyExecution.ts`、`privacyPortableData.ts`、`downloadSupplementaryMediaByRightsToken` 与对象存储实现。已有图片/视频单独获取路径要保留；历史对象缺失标为部分完成要保留。

当前聚合副本遇到 64 MiB 上限可能反复失败，而“联系客服分批”不能只停留在文字。沿用同一隐私请求与执行记录，补有界读取和分批交付：稳定分页、分片清单、数量和校验、本人访问、有效期、撤销、失败重试与幂等。不能直接提高上限让服务器一次性装载任意规模数据；不能新增导出中心或公共存储桶。

结果应准确区分：全部可交付、尚有分片、对象缺失、过期和撤销。同一素材重复引用不应无限膨胀；大文件仍须有真实可执行获取/人工受控交付路径，不以“请联系”替代交付。测试边界以下/以上、重复、缺失、分页变化、撤销和并发删除。

微信分享生命周期须实际验证：选择接收人、取消、微信切后台、返回、分享过程中再次打开隐私页。已交给系统的文件保留到回调；系统交接前变更身份或离页不得继续外发。

## F. 混合客服保留与旧备份防再现

读 `supportRetention.ts`、消息/素材关联、Worker 和现有删除抑制机制。R5 已修未支付取消、超时、无收货全额退款等终态时间及候选遍历，保留全部回归。

现实现同一会员一条会话，普通咨询与订单售后内容共享整段保留时钟。补最小消息/片段用途范围，不重建客服：普通咨询到期只处理该范围；交易关联消息、订单卡片、逐单退货指引及争议证据按相应期限保留。不能见到无 linkedOrderId 就武断认为是普通咨询，须考虑管理员回复和上下文；无法准确分离的历史段保守分类并记录依据。

同一附件有多个有效引用时不得提前删对象；最后有效用途到期才进入清理。保全并发、重复 Worker、半途失败、撤销保全、恢复旧数据库都必须可验证。到期处理与其他删除范围也要覆盖恢复抑制，不能只证明注销账号不会重新激活。

不要直接在生产试删真实历史记录。先隔离恢复、dry-run 统计候选与保护范围；真实不可逆执行按已明确授权范围进行。

## G. 退款、历史运费与已有权益

读 `aftersale.ts`、`aftersaleAllocation.ts`、退款预算/未知状态占用、佣金与权益冲正实现。逐件退款负现金已修，不重写正确算法。保留逐件、多次部分退款、累计不超原始现金/权益/优惠、并发申请、已退数量、未知结果占用和重复通知回归。

首发包邮，不增加新的运费业务。历史非零运费必须先核对是否真实存在及原订单合同快照，不能直接按当前包邮清零，也不能仅按商品数量分摊就认为所有原因/发货状态都适用。验证发货前后、消费者原因/质量原因、全退/部分退及累计边界。无真实非零历史数据时记录查询证据、保留兼容回归，不制造上线阻断；事实不明的旧单按原售后流程逐单处理，不能编造合同或扩大成运费中心。

关闭账号不抹掉退款、争议、未完成售后与历史权益；仍可本人核验进入受限历史处理，不能恢复成正常活跃会员。正式会员规则、权限、账务反向记账与已有权益保持一致。

## H. 原生 UX：完整操作，而非截图数量

依次使用原 AGENTS 的业务状态→微信原生能力→mobile-ui-ux-designer→design-taste-frontend/Taste→实测。读取本机实际可用 Skill，缺 Skill 不停工。Apple HIG 仅辅助触达、对齐、层级、可读性，不转换 UIKit/SwiftUI，不要求 Apple 开发者账号。微信官方 WeUI 可参考状态与弹层行为，不整体替换组件。无需为这些问题添加 GSAP、React 或重动画库。

在当前包完成购买、客服、售后、隐私和管理员旅程，并对其余已开放路由做状态覆盖：loading/empty/error/success、权限不足、慢网、重复点击、跳转失败、离页返回、草稿恢复。当前清单是 40 条路由，不再机械沿用“37页”。

优先补：客服从图片预览/系统选择器返回时不要无条件回到底部丢失历史位置；旧测量回调不得改写新页面；订单选择器旧订单分页能到达；真实相册/隐私弹窗触发 hide/show 时正确交接，不放宽身份检查。保留本轮移除订单卡片和跨身份修复。

视觉与文案：重要操作目标沿用 44px 实测点击区，不把 Apple 44pt 误换成 44rpx。正文、金额、错误和操作文字优先清楚可读，12px 只用于次要信息；不要让关键内容在小屏变成细小 rpx。保持系统字体、原品牌、稳定基线和行高，不靠 translateY 硬挪按钮文字。

去掉重复标题、重复库存、无行动价值的“准备中/已同步/请等待配置”及内部编号；未知结果说“结果暂未确认”，真失败给重试，不能用漂亮文案掩盖失败。每状态突出一个主要动作。退款金额、运费责任、数据保留、不可逆确认等必要事实不能删。长合同移至既有展开/协议入口，操作处保留关键承诺。

底部面板：标题、商品数量、问题选择和提交区可达；真实软键盘、小屏、大字、安全区不能遮挡。底部面板与全屏客服切换保留草稿和待确认发送，返回恢复正确对象。动画只在确需时小幅使用，不阻塞点击、不跳布局、不频繁全量 setData。全局 WXSS 接近仓库预算，采用局部样式而非塞入新库。

## I. 验收证据与发布候选

本轮包源码 SHA-256：`148d9a25ba86ebddd951e2046f518f2e75f9bfef121a433bd32ad56eca810638`。若你继续改代码，重新生成准确哈希。`c1246ea4` 历史模拟器资料已保存在 `source-acceptance-c1246ea46c4e.json`，不能当当前包验收。

`current-source-acceptance.json` 保持 blocked，直到当前包真的有证据。微信开发者工具验证不能冒称 iOS/Android 真机；注入键盘高度不是系统软键盘。当前包须记录工具/微信/系统版本、设备范围、操作与结果，关键成功、失败和恢复均有证据。没有设备可直接证明时明确指出，不生成假截图或把原生门禁改绿。

运行仓库现有 typecheck、unit、integration、package/route gate、contract/build/performance 等完整校验，分别读取结果。源码修复不得删掉保护测试；测试前置状态改变只准确更新夹具和真实 UI 动作清单。

## J. main 整合：不要与生产验证互相等待

把源码整合条件和公开发布条件分开。完成发布范围内真实源码修复、原检查和必要代码审阅后，在实际仓库保护允许时将原 PR22 转为 ready 并正常合并；不绕过 required review/checks、不制造自己的独立批准。合并后核对 main 自己的 CI、构建来源及包哈希。

不能因为生产必须来自 main 又要求 main 合并前已部署该 main 而形成循环。合并不等于公开发布授权，也不等于真机已通过。未通过的设备/平台项保留准确状态；业务源码缺口未处理则不能假装仅剩平台。

## K. 正确 production 部署与平台核验

读取实际环境和既有授权，核对目标实例、域名、长期资源/到期风险、数据库/COS、API/Worker、支付回调、日志与备份。最近回执的旧 release、APP_ENV=staging、未见实例 443 是待核实事实，不是本轮已证明的全链路结论。

不要只把 APP_ENV 字符串改为 production。先验证正式配置、权限、支付及存储适配，按 main 构建，完成真实备份在隔离环境恢复、迁移演练、旧数据兼容、删除/注销抑制重放，再按已授权方案迁移并切换 API/Worker。COS 私有副本及过期清理要实测；回滚必须同时考虑迁移兼容与资金/异步任务，不能重复执行未知退款。

检查公网 HTTPS/TLS 的实际路径（可能由负载均衡/CDN/网关承接），不要无差别开放端口。按现有证书续期方式验证；数据库不对全网开放。告警做独立触发和实际接收证据，不以进程 running 代替可用。

需要 Chrome/平台登录时打开准确页面，由用户本人登录/扫码后继续，不在聊天或日志索取/打印密码、密钥。分别核验微信公众平台小程序备案和云 ICP 备案，不把云控制台草稿当成小程序尚未提交。平台条件未满足不能绕过发布，但不阻止可完成的源代码、部署和体验准备。真实扣款、退款及不可逆历史清理仅在明确范围执行。

## L. 交付标准与停止边界

每批先复现→小范围修复→正反向验证→提交，更新恢复锚点。不要交反复出现且无人处理的“待测试、待批准”清单；可执行项直接执行，真正外部项只列最少事实和具体动作。无重大确定问题不要趁收口改造其他模块。

最终分别给出：
- 源码：branch/HEAD/main SHA、原 PR22 状态、main 自身 CI 与构建。
- 原生：当前包哈希、当前版本模拟器和实体 iOS/Android 实测范围，未通过项明确。
- 生产：真实目标和运行版本、迁移、隔离恢复、防再现、COS/API/Worker、回滚及独立告警回执。
- 平台：准确备案系统、版本审核/发布条件、尚需用户登录/审批的最小动作。

目标不是保证永无未知缺陷，也不是继续添加功能。目标是现有承诺有正确成功路径、异常可恢复、数据与金额不乱、用户界面准确易用，并有与最终版本相匹配的交付证据。

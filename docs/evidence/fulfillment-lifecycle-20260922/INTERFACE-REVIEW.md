# 履约新增8个方法的13维复核

范围：PR22的8个新增HTTP方法及shipping worker；源程序HEAD `0f1b4c346173ed26d6ffd3d6e3612ef893b14212`。以下是源码复核和已有测试事实，**不把服务层测试等同于全部HTTP/平台/设备验收**。全工程分母234个/v1方法，含health236；完整13维验收的总数仍不能填成236/236。

|维度|复核事实|证据/局限|
|---|---|---|
|PRD业务|§8.2、ORD-02/03：R0单包裹全部行；支付、交寄、收货、退款独立|首发不支持拆分发货；不静默处理部分退款后发货|
|身份|全部8个方法在既有认证/有效会员前置检查后运行|203个保护方法逐一缺失/无效认证单元门；授权HTTP全路径还需补|
|能力|管理读/发货/批量/导出均需commerce.fulfillment.manage；grant/member共享锁与到期检查|服务层无权、撤权竞争验证；正式操作员登录仍缺|
|对象行|会员查询/确认必须拥有原订单；管理员发货锁原订单并核对支付事实|外人订单查询404；跨账号原生迟到读丢弃|
|字段|会员仅本单运单/快递/状态；管理列表不解密地址；导出按授权解密固定字段|运单加密；审计无明文PII；完整主体导出/删除尚未覆盖|
|状态动作|待支付不发货；退款申请/处理中/异常/成功退款阻止新发货；已发货不可新建第二包裹|签名合成支付/退款回执实测；平台签收/物流异常接入仍缺|
|环境|功能配置默认不启用；网络另需保护授权文件；staging按隔离订单白名单|没有实际安装微信出站批准文件，没有真实资金或production变更|
|输入|UUID、版本、ISO时间、carrier/运单字符范围；批量25；导出500；过滤键拒绝未知|格式校验不等于承运商已接受该运单，真实轨迹仍待接入|
|幂等版本|原actor/key/内容指纹；并发同键同结果，换运单冲突；显式收货独立key/version|批量逐行原子；页面未知结果沿用原key；任意xlsx文件解析未实现|
|并发|当前authority锁、原order行锁、SERIALIZABLE重试；退款共享订单锁定边界|测试并发重复/撤权；完整发货与退款争用压力尚未执行|
|DB及外部|原子shipment/lines/event/sync/audit；所有微信网络在事务外；dispatch-before-network journal|无自动重传；manual_review人工恢复操作仍待完成|
|限制/时间/分页|共用operation budget、限流；50/100游标列表，500导出；token15s/有界JSON|批量最坏25事务可能超过HTTP截止后部分成功，原key逐行可恢复；需staging压测|
|正反证据|800全量集成+14修复回归、1018单元+4原生生命周期，真实API原生2图|未覆盖完整已发货原生/物理设备/实际微信物流；不能标发布通过|

## 必须继续完成的工程

1. 正式售后对象状态与退款关联；当前仅已有受控退款命令和本批发货阻断。
2. 官方物流助手可用性/绑定核验、轨迹/状态事件、真实签收后的提醒确认收货；无付费第三方硬依赖。
3. 管理端登录、批量成功/部分成功的HTTP及UI全链路验收、微信同步人工恢复。
4. 全域隐私主体投影/导出/删除执行及批准保留策略；当前129表只是映射。
5. 当前候选staging完整交易验收、iOS/Android真机以及所有236方法逐维证据补齐。

## 物流助手进一步核验（官方文档读取，不等于CISME账号开通）

2026-09-22读取[获取绑定物流账号](https://developers.weixin.qq.com/miniprogram/dev/server/API/express/express-by-business/api_getallaccount.html)：服务端GET `express/business/account/getall` 返回账号数量、delivery_id、biz_id、绑定状态、面单余额及服务类型。后续只读核验应最小投影承运商/绑定状态/余额是否可用，不把账号备注、客户编码或token写入发布证据。

[支持快递公司列表](https://developers.weixin.qq.com/miniprogram/dev/server/API/express/express-by-business/api_getalldelivery.html)：GET `express/business/delivery/getall` 返回delivery_id、名称、散单能力及服务类型；支持列表不是CISME已签约或已开通证据。两接口注明仅认证小程序可用。散单不代表运费免费；本轮未下物流订单、未绑定公司、未购买服务。没有引入第三方物流SDK。

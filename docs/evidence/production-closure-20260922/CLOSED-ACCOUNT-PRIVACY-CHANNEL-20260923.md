# 已注销账号的历史隐私请求通道（2026-09-23）

本批在原小程序和原隐私工单上继续施工。已注销的微信账号可用一次新的 `wx.login` 码核验原微信身份，取得 30 分钟、仅限隐私请求列表、提交和本人补充的凭证。服务端每次使用该凭证都复查原微信身份、AppID、会员归属和 `deleted` 状态；凭证不能访问会员资料、客服、导出副本或管理端。该流程不恢复会员资格。

小程序将受限凭证与正式会员凭证分别保存，并只给隐私请求路由附加受限凭证。切换凭证后拒绝旧请求的迟到结果。已注销账号的隐私页仅展示仍可使用的受理、回复操作，不展示无法读取的旧资料副本按钮；工作人员继续在现有隐私待办处理。

验证：隔离数据库 `privacy-rights.test.ts` 15 项通过，覆盖正式形态微信身份核验、本人请求、管理员回复、会员接口隔离和状态改变撤销；相关单测 85 项通过；完整单测 1251 项通过、1 项跳过；TypeScript、252 个 `/v1` 接口契约、两个 WXML 原生编译与包检查通过。当前原生源码哈希为 `0830bb425716dd46aafdc66187020000c68766b722a912cf7a39d971adced87a`，视觉清单仍为 `blocked`，没有声称真机通过。

范围：本批解决历史隐私请求的重新进入和权限隔离。正式身份全量导出、删除、注销执行及备份恢复防再现仍在后续施工范围内；本批不可作为正式隐私服务已闭环或发布门槛已完成的证明。

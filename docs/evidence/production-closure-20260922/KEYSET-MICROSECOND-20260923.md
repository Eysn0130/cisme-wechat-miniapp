# 时间游标精度复核（2026-09-23 UTC）

| ID | 范围 | 复现与风险 | 状态 | 修复与证据 | 后续 |
|---|---|---|---|---|---|
| PAGE-US-01 | 售后案件分页 | PostgreSQL `timestamptz` 保留微秒，`Date.toISOString()` 只保留毫秒。第一页游标截断后，创建时间落在同一毫秒且略早的下一条案件被 `<` 过滤，历史记录漏页。 | Fixed（隔离集成） | 在隔离库直接创建两个同毫秒、不同微秒的已撤回历史案件；原实现第二页返回错误案件，改为 SQL 返回 UTC 六位小数游标后通过。游标仍按成员/模式/订单绑定。 | 原生页面仍需准确包的微信工具与设备验收。 |
| PAGE-US-02 | 退款历史和审批队列、成员结算和审批队列、资金异常、账单批次、履约证明队列、隔离购物权益列表 | 同一 `finishPage` 模式用 Node 日期构造游标，存在同毫秒漏页。 | Fixed（源码与全量集成） / Verification gap（逐入口边界） | 各查询从实际排序的 `timestamptz` 列生成 UTC 微秒游标；`npm run typecheck` 和独立临时 PostgreSQL/S3 的 836 项集成测试通过。未将通用构造器或数据库日期解析方式全局改写。 | 对高频资金/管理列表补同毫秒逐入口回归、运行时能力/对象隔离与真机滚动验收。 |

此批仅涉及 API 与测试，小程序源码包仍为 `55a8abca05ad5dd5ab9b1c68a2e4a1bc73470ef7b180445fa776c08277a6b9cc`。集成测试通过不等于接口 13 维全审查或生产数据验收。

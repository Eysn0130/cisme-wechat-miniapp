# 正式交易源码候选（未开放真实交易）

本批基于 0c3ce8d2156d4976e188f0da12fdc00ccd77ab82。没有生产授权文件、真实网络支付/退款、部署或微信发布。

补齐现有正式协议的消费路径：商品目录、报价、订单建立、JSAPI 下单、关单、退款申请/批准及 Worker 提交。使用原订单/付款者绑定、验签回调、查单、幂等和队列；未建立第二支付体系。production 只能选择已审核并发布的 admin 商品，合成/历史预览商品不能成交。整数分与首发包邮承诺沿用现有规则。正式转账、购物权益抵扣没有因此开放。

新交易需要独立、保护文件中的逐能力授权，绑定 production/AppID/商户、有效期和批准引用；同时必须具备查单、回调及账单恢复授权和有效匹配商户证书。恢复授权不自动开放新交易，文件路径和商户凭据也不是批准。固定官方 HTTPS 普通商户接口、禁止重定向、逐请求复核、日志不含订单正文或凭据。没有创建真实授权文件。

小程序识别 version 2 正式能力契约；撤回新交易授权后仍保留可用的历史核对入口。管理 UI 仍只有小程序，PC Admin 不进入正式制品。本批没有部署管理网页。

## 验证范围

- 全量单元：1072 通过、1 跳过，97 文件。
- 全量集成：806 通过、45 文件；独占新建 DB/S3 run `7eb84efec405ab041970fc85`，已清理。签名渠道均为 loopback 合成 Provider。
- 定向集成：49 通过。覆盖正式形状配置装配、合成商品拒绝、已审核 admin 商品下单、授权撤回、历史读取/回调继续处理；不等于真实商户资格/资金验收。
- 类型检查、build、contract 通过。237 个 /v1 方法与 OpenAPI 一致，加健康检查共 239；这不是全接口 13 维已完成。
- 本次小程序源码哈希 `e061bad98beb3cfa6eaea83b28e4dd97718003e728756db59ab94239cc275178`。开发者工具刷新命令成功不代表完整原生验收；37 页状态矩阵、iOS/Android 继续 blocked，旧截图仍仅绑定其原源码。
- CI35744648322 对 0c3ce8d 的结果为失败：805 项集成通过、1 项费率生效时间断言失败。根因为该断言依赖运行时与上海午夜关系。改为固定 48 小时后的申请时间应原样保留，同时保留已逾期申请顺延验证；未放宽业务逻辑。

## 仍需继续

完整正式交易验收、真实平台绑定与权限、原生有效对象/真机、售后与隐私执行、全量接口复核、production 保留数据部署、告警送达及 COS 恢复。新发现旧 SQL 自带事务与部署迁移器事务嵌套问题，下一批单独复现修复。没有将这些源码/技术缺口转写为运营截图义务。

依据：微信支付官方普通商户 JSAPI 下单 https://pay.wechatpay.cn/doc/v3/merchant/4012791897 ，小程序支付指南 https://pay.wechatpay.cn/doc/v3/merchant/4012791911 。只复用现有 TypeScript 协议，无新增 SDK。

## 原生查单边界修复（2026-09-22）

复现：formalRecoveryAvailable=true 而 formalMoneyOperationsAvailable=false 时，订单页将 GET 查单和 POST 付款共用资金开关，导致无法核对原单。现将查询能力单独计算，保留已核验订单、当前身份、页面可见性及 runtime 校验。隐藏、切换身份、核心刷新、runtime 失败时关闭入口。付款、关单、退款仍独立受资金开关和服务端授权控制。纠正管理中心和取消/恢复对话框中不准确的“隔离测试/不会真实资金操作”文案。

定向 93 项通过；本地全量 1077 项通过、1 项平台条件跳过，build/typecheck/contract 通过（237 个 /v1 method、239 个含健康检查的 method、32 个事件）。初次全量在更新源码哈希前启动而失败；更新诚实 blocked manifest 后重跑通过。新原生包哈希 728cdc6cdfffe531f53f102fc6ca1a9db1dda6d939beeb14cb197aed14768853；没有将旧截图转记为本次原生/真机通过。

# 隐私请求往返与管理待办源码回执（2026-09-23）

本批沿用现有小程序隐私请求及管理页面。受理人员可明确选择下一步由自己处理或请用户补充；用户只能对属于自己的、明确等待补充的请求提交说明。补充以独立不可变记录保存，幂等重试返回原回执，版本变化、越权和不匹配的请求编号被拒绝。用户补充后，原请求回到现有管理队列；等待用户期间不反复占用新待办。数据库约束阻止将等待用户的请求错误推进到执行计划。

验证：本机 `npm run typecheck`、`npm run lint:contracts`、`npm test`（1245 通过、1 跳过）、`npm run test:integration`（858 通过）、小程序包检查及 `npm run restore:verify` 均通过。恢复只针对独立合成 PostgreSQL 与合成对象，迁移前后各 83 项；不是生产备份恢复。当前原生包哈希为 `d059bab315b27f47a1b19212f01ec8781d0d6e1b6efccbce3e8be578d0aa2275`，没有 DevTools 或真机逐态实测，不计为原生通过。

本批没有把回复冒充导出、删除或注销。正式身份的完整执行器、分类保留和备份防再现仍未实现；当前 `SyntheticPrivacyExecution` 仅服务于隔离测试。PR22、main 与 production 状态须以实时仓库和服务器记录核验。

# Live production observation collector

The original target guard checked caller-supplied JSON; it could not establish where that JSON came from. production-observation.py now reads the fixed host metadata fields, hostname, DNS and protected actual configuration itself. It imports and calls production-target.py for candidate checks rather than duplicating or weakening that guard.

The authenticated Lighthouse production TAT session showed lhins-61ikz4mi maps to metadata instance ins-l9utqwxv, private10.0.0.10 and public124.223.74.198. The pinned mapping is in PRODUCTION-LIVE-METADATA-20260923.json; mismatch, replacement host or extra/wrong DNS answers fail closed. The collector does not mislabel the underlying metadata ID as the Lighthouse ID. Official field references: [Lighthouse DescribeInstances](https://cloud.tencent.com/document/api/1207/47573), [instance metadata](https://cloud.tencent.com/document/product/213/4934).

The actual read-only run at 2026-09-23T02:08:16.605883Z produced PRODUCTION-OBSERVATION-20260923-RAW.json (640 bytes, SHA256 9943c206833699b6b77ecd0e1c36a7b8c9f4b627a383dc1f6e0aad6c5cc9c039). It confirms original cisme / loopback5432 / COS lhcos-81ddf-1257392443 and the incorrect live APP_ENV=staging label. No full URL, password or storage credential was emitted. No configuration, service, database, firewall or credential changed; only a uniquely named read-only tool directory and sanitized receipt were created under /home/ubuntu.

Candidate validation separately refuses a changed database URL or storage binding, requires production label and exact reviewed main SHA/tree, and returns permissionGranted=false/deployed=false. This does not rotate a leaked credential implicitly during an upgrade. Configuration reads refuse symlinks, non-regular/oversized, world-readable and group-writable files; CLI failures use fixed codes without raw parser errors.

Thirteen new offline tests passed, including wrong host/DNS, original-resource binding, quoted URL parsing, protected-file handling, credential-rebinding rejection and invoking the existing target guard. The complete Python set is now61 staging/target tests plus4 security-audit tests, all passed locally.

Limit: this is the live collection/guard component, not a completed production deployment transaction. A real preserve-data upgrader still must call guard_for_upgrade immediately before migration and activation, bind independently verified main/CI and artifact provenance, verify backups/restores and migration compatibility, manage API/Worker cutover and preserve new writes on rollback. The candidate guard was not passed against an invented production candidate configuration. Production remains the old 20260909-native-login release with unknown source SHA. No migration/deployment approval is inferred from this report.

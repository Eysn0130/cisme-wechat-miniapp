# 正式身份商业会员路径核对（2026-09-23）

隔离数据库使用仅有 `wechat_miniprogram` 身份的账号及 `production` 权限环境。`formal-commercial-identity.test.ts` 证明现有成员服务可由管理员明确指定资格到期日，工程 12 个月选项在正式模式被拒绝；迁移内 20% 工程默认费率不被当作正式规则。管理员可提交任意分位精度的正式费率，独立复核后成为将来生效的规则。使用已复核且到达生效时间的费率，会员推荐码、用户首次确认和幂等重试可完成，管理员详情读取实际费率。测试数据库没有 `dev_test` 身份。

这是服务层正式模式的成功路径验证，未使用真实微信用户或扣款。它不证明正式服务器已经录入经营方决定的费率、微信原生页面已完成验收，也不证明支付、结算与提现已开放。正式规则须由现有管理端录入并按实际生效时间核对；不能把隔离夹具费率或已批准但尚未生效的提议当成现行事实。

当前原生工具只读检查：开发者工具已登录且导入的是 `/Users/mini/CISME/apps/miniprogram`。`pages/management-member/index.wxml` 模板编译返回成功；随后工具返回每分钟频率限制，其他模板和样式未取得编译结果。未移动模拟器当前首页，未生成当前源码的页面或真机验收证据。

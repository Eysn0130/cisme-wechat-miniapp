# 2026-09-23 保留分类与已注销账号登录保护

## 已落地

- `202609230010_retention_classes.sql` 根据经营方本次确认，声明交易及其关联客服记录的 36 个日历月基线。保留期限按类别存储，修改留审计；未结争议及更长法定义务仍须在执行前判定。两个新类别保持 `declared` / `active=false`，不声称已有自动清理执行器。
- 原客服会话可能同时承载普通咨询和订单、售后证据。关联订单或售后的会话被明确归入交易客服类别；原整段普通咨询清理入口不能清除它，避免误删交易事实。后续应按消息及关联对象完成分段到期执行，不能靠启用普通咨询策略跳过这一边界。
- 已有正式或测试微信身份再次登录时，读取并锁定成员状态。`deleted` 账号返回 `ACCOUNT_CLOSED`，其他非活跃状态返回 `MEMBER_NOT_ACTIVE`，不签发新会话或写入新的同意记录。该守卫不等于完整注销或备份恢复防再现。

## 验证范围

- 隔离数据库的客服集成用例覆盖交易分类、普通保留规则激活后仍拒绝整段清理、期限单位互斥约束及策略更新审计。
- 身份集成用例覆盖非活跃及已注销账号不能再次登录、同意记录数量不增加。
- 本地 `npm run typecheck`、`npm run lint:contracts` 通过；`npm test` 为 1245 通过、1 跳过；`npm run test:integration` 为 858 通过。`npm run restore:verify` 在自有合成数据库中完成 85 次迁移前后核对与对象恢复。合成恢复不代表生产备份。

## 仍须完成

正式身份的全范围导出、删除与账号注销执行器；分段客服保留执行；交易到期与法定义务/争议判定；备份外独立墓碑与恢复重放。这些是源码缺口，不能用“已声明策略”或“待用户批准”替代。

# 推荐码页面与正式费率可用性 · 2026-09-23

原邀请页写死“默认 20%，调整至 35%”和“真实支付未开放”，与现有管理员费率的正式生效规则无关，且缺有效费率时可能长时间显示“正在生成”。本批删除静态经营比例；`/v1/me/commercial-membership` 返回当前推荐关系是否可用，生产模式只认已生效、非迁移种子的费率，现有推荐码仍可回看但在规则不可用时不能复制或继续分享。未建立过推荐码、也没有可用规则时不展示可选推荐卡；会员资格及已有佣金记录仍通过原页面读取。服务端确认推荐关系时仍重新校验费率，页面状态不构成授权。

验证：类型检查、小程序包门禁、完整单元测试（1248 通过、1 跳过）、原商业会员隔离集成用例（8 通过）。隔离用例使用正式形态微信身份和 production 费率权限，覆盖规则缺失、未来生效及当前生效的正反状态。当前小程序源码 SHA-256：`600f8867aa7b0534e00c2317de62d47c2c779e0a36bf1f4c154a30458a77e57a`；已更新阻断状态的当前源码清单并归档前一包清单。没有新增当前包的微信开发者工具、iOS 或 Android 实测证据。

# 隐私请求可达性收口（2026-09-23 UTC）

| ID | 范围 | 复现与风险 | 状态 | 修复与证据 | 后续 |
|---|---|---|---|---|---|
| PRIVACY-PAGE-01 | `PrivacyRights.list`、`queue`、三个 GET 路由、用户与原生管理页 | 旧接口固定 `LIMIT 100`，原生页面只请求首批；隔离库加入 110 条历史请求后，第 101 条没有可达入口。 | Fixed（源码与隔离集成） / Verification gap（微信原生） | `page=1` 采用每页 30 条、数据库微秒精度时间与 UUID 的 keyset 游标；旧无参数数组响应保持兼容。两个原生页面提供后续页、重试和刷新；管理每页重新核验能力；隐藏或身份变化清除私密内容。`native-privacy-management.test.ts` 覆盖超过 100 条、去重、权限与坏游标，页面单元测试覆盖迟到响应。 | 用当前小程序包在微信开发者工具与获授权真机实测加载、长文本、滚动和切号。 |

本轮包哈希 `89871afeefacc3cad25de7559bb8b71fd63fb5c047c5989890405d308a4206d1`，40 路由和 iOS/Android 原生验收继续 blocked。`source-acceptance-6b27f690ba07.json` 是上一个包的原始清单归档；更早截图不转移为当前包证据。

分页队列按当前状态与办理期限排序。并发状态变化可能改变跨页位置；客户端按 ID 去重，工作人员可刷新首批重新核对。此修复解决记录可达性，不证明正式数据导出、删除、注销执行已具备生产范围。

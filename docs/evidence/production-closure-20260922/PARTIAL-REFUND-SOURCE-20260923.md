# 部分退款源码回执（2026-09-23）

本批在原 `codex/fulfillment-lifecycle-20260922` 分支继续原订单、售后、退款与小程序订单详情实现。按订单行和数量记录不可变的售后申请分摊；原单折扣后的商品实付金额、权益支付、运费和计佣现金按累计数量分币。成功的上一笔退款保留原案件事实，并释放剩余数量供后续申请。未决退款持续占用预算；订单行锁、幂等键及退款意图的累计上限阻止并发超额。退款仍经原审批、渠道回调、权益退回和佣金冲正链路。

验证范围：`npm test` 通过（1243 项单元测试，1 项平台相关跳过）；`npm run test:integration` 在一次性 PostgreSQL 18.4 容器通过 855 项、49 个文件。`npm run typecheck`、`npm run lint:contracts`、原生包检查及 `git diff --check` 通过。定向 PostgreSQL 16.15 测试通过。集成测试覆盖两件商品逐件申请、优惠与运费累计、未决占用、数量超限、归属、幂等冲突、并发单赢家、签名回调、最终余额与佣金冲正；分摊单元测试覆盖多订单行与舍入守恒。

这只证明隔离环境的源码路径。真实支付、生产数据、微信开发者工具和真机均未用这批代码验收。当前原生包 SHA-256 为 `22f2296b402a522b5cee9964865061c9c84be956ce6b3fa3da11341c823a8701`。原系统要求每次退款至少有一分可原路退回的现金；全部由购物权益支付且本次数量分摊无现金的边界仍需完成单独的权益退回路径，不得称为已闭环。

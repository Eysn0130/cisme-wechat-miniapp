# 资金查单准入与隐私下载锁序

起点 main `78081da1e1c4cbae6e6ede8f98c8ec4cdccb51ca`（PR18 已合并，准确 main CI 35695487525：986 单元/771 集成）。本切片修复两个已复现问题，不声明正式发布准备完成。

- 管理员支付/退款/转账查原单此前只做事务前权限检查，独立连接中尚未提交的撤权/封禁会留下可利用窗口。现在当前 capability、active member 与原对象存在性在事务中复核，准入审计随事务提交；之后才调用原签名 Provider，避免自动事务重试重复渠道请求。审计 action `commerce.money.recheck_admitted` 只表示准入，既不表示请求实际发出，也不表示资金成功。提交后撤权不撤销此前已准入查询。
- 下载 join 的隐式锁序可能先持 artifact 再等 job；清理先持 job 再删除 artifact，已用独立连接复现100ms锁超时。现在下载先显式锁本人 job，再进行其余主体、文件、状态、双TTL校验；没有放宽锁预算。

新增11例（7个并发反例、3个真实签名合成渠道正例、1个无效kind/缺失对象批量反例）。渠道handler从另一个数据库连接确认审计在I/O之前已提交；拒绝路径断言无渠道请求、无payment/refund/transfer事实及无准入审计。64项针对性集成 PASS；完整本地985单元PASS+1平台条件SKIP，782集成PASS（45文件），build/typecheck/contracts PASS。新建隔离run `1dfa3bf9453c249f93deee5b`，只操作本轮所属DB/S3，均清理；未触碰旧cisme_test。

第一次隐私反例fixture触及每主体10次请求限制，在并发断言前被拒；改用新建合成主体后才复现锁冲突，未放宽配额。所有失败/通过阶段保留脱敏摘要及来源run；源码哈希见verification.json。

2个HTTP方法的13维记录为限定切片；下载方法与PR16重叠，累计不同方法28/228，完整13维验收仍为0。原生文件未修改，也未重新原生验收本批后端；前端包哈希仍a296da16a0edafe3d1fc988b94539a52a23f2d7e8b1fa85d9702e96b4276161c。

正式交易/履约、完整隐私主体resolver/政策执行、其余方法审阅、原生全状态与真机、批准目标部署、告警和恢复仍未完成。隐私受理回复与approved/executing/failed状态转换仍需继续审阅，不在本修复范围。服务已开通，具体绑定/权限/批准政策及目标仍待核验；备案等待不替代工程。旧cisme_test事故：影响UNKNOWN、未恢复。

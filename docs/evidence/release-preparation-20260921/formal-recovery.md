# 正式恢复接线的隔离验证

本批完成保护文件授权、正式 HTTPS 历史读取 transport、API 和独立 worker 的装配。默认没有 grant；资金新命令继续关闭。独立能力/配置/限制见 `infra/payment/README.md`。

本机反例覆盖错误环境/AppID/mchid/模式、过期、权限重复/越界、文件权限/符号链接、删除 grant 后撤销、非白名单目标、所有写方法拒绝。真实 RSA/AES 的合成 HTTP 集成证明：staging 配置的独立 payment.callback grant 能接收已知合成订单的签名通知，同时新预支付和未授权查单仍拒绝；删除 grant 后回调不再 ACK。没有传递非 test 的 synthetic fetch 注入。

702 个现行集成测试通过；本次所有数据库均由一次性启动器创建并清理。目标测试 29/29 通过。安全库存、退款、结算、账单异常用例沿用现有真实 PostgreSQL + 本机模拟渠道，不据此声称正式资金已验证。

正式商户模式、绑定、证书/序列号身份独立核验、凭据本机装配、实际代理原文保留、平台权限及正式查询均 NOT_RUN。新资金 Provider、生产订单/履约与订单发货管理装配仍属源码缺口，尚未完成；不能转写为负责人补截图。真实资金验证及正式发布未经授权、未执行。releaseReady=false。

# 发布准备恢复锚点与分类账

`releaseReady=false`。支付已开通、备案审核中是用户本轮明确报告；不再询问开通状态。旧 cisme_test 事故影响未知、未恢复；恢复合成目标的成功不能更改事故状态。

2026-09-22 后续实时核验：Chrome 腾讯云访问及已有 TAT 控制通道已恢复，staging 实例/IP、校验 HTTPS 200、旧部署与隐私版本漂移已核实；详见 [环境解除阻断记录](../../launch-unblock-20260922/README.md)。旧的“云控制台超时、目标身份未核实”只保留为历史。本机代理路径、续期防火墙、批准的全新隔离部署和正式政策仍未关闭。

## 源码与工程尚未完成（由施工继续承担）

1. E：228 方法已映射并命中 HTTP，195 方法已验证两类入口拒绝；每个接口的 PRD 稳定验收 ID、对象/字段/动作、版本/幂等、并发、副作用和非路由后台面仍未全量签结。具体已审查切片和开放维度见 `audit/reviewed-slices.md`。不能改写为等负责人截图。
2. F：正式历史支付/退款回调、查单、账单恢复已具独立授权装配；正式新订单、预支付、退款发送、转账、生产履约/订单发货管理适配尚未完成。原有合成报价/库存/Inbox/Outbox/原号恢复不是这些正式装配的替代品；不得只改 paymentAvailable 或删环境门禁。
3. G/H：六类本人只读命令发现和 Page 归属修复已落地，原生登录失效恢复与四步护理/感受保存已实际观察；535 项适用状态的完整原生验收、跨设备只读恢复、存储故障/大字/弱网等仍需持续实现与测试。缺对象参数的页面仍需补有效 fixture 后验收。
4. I：125 表的字段/主体候选图已生成并受迁移漂移检查；实际可执行合成导出仍是明确的小范围白名单。完整主体 resolver、多主体脱敏、非 FK/多态对象关系、外部对象保留/导出/删除执行尚未完成。生产删除保持关闭，不能用一张 FK 图宣布完成。
5. J/K：不可变后端候选、72 个前向迁移、新建合成目标上的重复迁移/打包 runtime 检查、合成 DB/对象恢复已完成；真正部署后的 raw-body 代理保真、多实例监控/送达、生产备份恢复、版本回退和最终材料绑定仍需目标环境验证。现有包不含环境秘密，也未部署。

## 负责人或平台必须提供的最小事实（只阻断对应外部动作）

| 项目 | 最小补充 | 当前为何不能当通过 |
|---|---|---|
| 商户与小程序绑定 | 已授权本机配置/凭据的受保护路径引用；实际普通商户/服务商模式；AppID/mchid 精确绑定及分产品权限的核验结果 | 支付开通不能证明绑定/退款/转账全部获准。不要在聊天中发送密钥或订单原文 |
| 隔离体验目标 | 已批准的 HTTPS API 或已有云目标、AppID、体验成员范围、域名/隐私指引核验引用 | 源码 staging 域名仅配置文字，不证明当前目标隔离或已部署。本机 IDE 合成验收已先行，不要求全部真机证据才开始受控体验 |
| 真机 | 获授权的 iOS/Android 和第二设备实际操作，绑定当前包哈希与状态/环境 | 模拟器 242×524 原图、编译与 CI 不能替代真机；不放大伪造分辨率 |
| 业务与隐私政策 | 商品资质、运费/售后/履约、佣金/税费、隐私保留/法律保全及正式文本的有效版本/负责人批准引用 | 技术团队不能编造费率、法务批准或保留期限 |
| 备案/正式审核 | 平台备案最终结果、类目/资质等实际结论 | 审核等待仅阻断正式放行；不阻断本机工程 |
| 真实资金验证 | 后续精确订单对象、金额上限、次数、环境、操作人、原号核对/退款与停止条件的单独批准；由负责人手动执行 | 本轮未提出可执行真实对象、未调用真实付款/退款/转账；小额/staging 不是通用 sandbox |
| 值守和恢复 | 目标 topology、受控配置/备份位置、告警接收人及真人响应演练授权 | 聚合监控代码不是通知已送达，合成恢复不是生产恢复 |
| 独立复核 | ChatGPT/GitHub 对最终准确 HEAD 的独立结论 | 当前施工者不是独立审阅人；没有伪造 approvals 或修改仓库规则 |

## 再开工顺序

先 fetch origin，核对 PR/main 与本地未提交及同根任务；不要回退到研究锚点或重复已合并补丁。任何破坏性合成测试只由 `scripts/disposable-test.mjs` 新建归属 DB/S3；旧端口 55432/58333 不是测试目标。保留全部 unknown 原键和渠道事实。新版本的原生包哈希变化时，不复用旧截图为当前全状态证据。

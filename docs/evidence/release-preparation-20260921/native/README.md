# 本轮原生工程与观察（非发布验收）

基线为 PR10 合并 main `3c9da5eb18972858df376dc0f7cc89c6343c6006`。
本批接入六类本人历史命令的只读发现：订单页 refund/cancel/cancel-verified、佣金页 settlement/credit/credit-cancel。列表为空不证明失败，不还原幂等键、载荷或自动解除未知命令。分页失败保留已读事实；会话、环境、Page 世代变化丢弃旧响应。

management-members、community-author/post/compose、support、management-support-chat 的 GET 归属 Page 并在隐藏时取消；写命令继续由原有幂等与结果未知恢复处理。详情首次读取、草稿尚未获得 postId 时的返回恢复另有定向用例。

## 四层证据

- 源码包与模板：当前最终包 `003a1a24ce2e13fe4303c8bcad4456dc6263fc8b5d6319e04fff7f1d3b67dcdf`，259 文件/37 路由。订单/佣金 WXML 实际开发者工具编译成功；这两处编译发生在随后两个社区恢复修复前。
- Page 行为：单元测试验证六类发现、分页/异常、存储不可用、账号/ABA/环境切换及六页面隐藏取消；不等于实际 wx 存储故障或设备重启实测。
- 本机 IDE：`capture-manifest.json` 将 36 路由单状态截图绑定**采集时**的包 `27528d...`；随后两个社区恢复修复令最终包改变。`observations.json` 只是导航、loading/error 和少量状态观察，未把自动观察当视觉全通过。原图 242×524，未放大，未满足正式截图尺寸门禁。抽查订单、佣金、客服、草稿原图，当前首屏可读；命令列表位于下方，首屏截图不证明其滚动布局。
- 真机/跨设备/全部适用状态：NOT_RUN，完整矩阵 0/37；account 本次只经原生本地测试协议勾选和登录按钮操作，没有纳入截图 sweep。没有团队体验版、正式提审或发布。

后续技术项仍包括逐页适用状态矩阵、真实原生存储故障与中断恢复观察、动态业务对象夹具补充及当前最终包重新验收。设备操作不能替代这些源码/夹具工作。

# 2026-09-21 发布准备施工（持续更新）

入场 main/HEAD `9c71c7479890c0d7fee6a7022b51c7f6980feb75`，tree `6fa22c3765b948ff7e4d3765b99cd0c41ffa8153`。唯一工作树 `/Users/mini/CISME`，入场干净，实时无开放 PR，同目录另一 Codex 任务空闲。没有启动 Goal，没有重放 PR4–9，没有操作旧 Docker 目标。

本次 ZIP SHA256 `01b0aa8b4783a17190aedc97ed354c0ca4daae8eae545587f1511c0ba8b1047d`：7 个唯一条目，总计 57,016 bytes；无路径穿越、链接、加密条目；6 项 SHA256 清单匹配。直接以 zipfile 读正文，未执行附件或覆盖工程。完整读取执行 A–M、研究记录及 reference 两份报告；附件中的历史统计不是本轮结果，施工授权来自用户本轮请求。

用户最新状态：支付已开通、备案审核中（用户报告，非后台独立核验）。商户绑定、正式接线、真实资金测试、发布批准分别未知/未通过。`releaseReady=false`。原 cisme_test 事故的数据影响未知、未恢复，安全工具修复不改变这一事实。

## C/D 第一批

- 一次性启动器每次新建 Docker 实例、随机 loopback 端口、库、角色和能力令牌，记录不含密码的归属；测试子进程继承目标，退出仅删除相同 ID、名称、run label 的本次容器和匿名卷。
- testkit 重置前检查显式目标、非旧端口、run/role/db、实例启动时间与端点、用途/重置许可；专用连接持有 advisory lock，拒绝并发 reset。integration setup 在测试文件及清理 hooks 注册前验证目标，覆盖直接建 Pool 的消费者。迁移无默认目标、合成 seed 仅允许已验证一次性目标。CI 复用原有流程，每个集成/性能执行创建一次性 DB；历史性能基线仅被同一启动器包裹，不改变其应用代码。
- 204 空响应与 JSON 均必须验签；AAD 仅 undefined 归一化为空，null/对象等仍拒绝；真实合成 RSA/AES-GCM 反例修复前 5 失败、9 通过。账单下载保持独立的签名申请＋摘要验证，不机械套 JSON 验签。
- 通道普通响应流上限 1MB、账单 10MB，在读取过程中限制。转账协议需显式场景资料；测试场景文字仅由 test-only 结算服务提供，未开放正式转账。
- 定向用例覆盖无签/坏签/未知公钥/过期/正确空签名、省略/空/非法 AAD、损坏 GCM tag、chunked 大响应取消、无场景禁止出站。正式默认 transport 仍关闭。

验证日志暂存本机 `tmp/release-preparation/`；准确提交、CI、最终计数在后续回执填写。此前一次新建目标集成 702/702 已通过并清理；最终批次仍以 final 日志与新 HEAD CI 为准。单元/合成/编译不是原生真机或正式发布。

## 尚须继续（不是 Owner 截图问题）

E 全接口对象/字段/动作/并发副作用逐项审计；G 原生六类本人命令发现及剩余生命周期；F 可受控正式 transport/provider/worker 接线与异常链路；I 隐私主体联结、白名单与治理；H 当前包原生适用矩阵/受控体验；J 部署、告警、恢复与配置；K/L exact SHA 候选、材料和 CI。各项只按实际证据升级，不能以本文声明完成。

外部最小项另列：备案结论、商户/AppID 绑定和分产品权限、获授权环境/设备、审批业务政策、负责人手动真实资金验证和正式放行。正在等待外部事实不阻塞上述技术工作。

## 第二批：原生只读恢复与 Page 生命周期

详见 [native/README.md](native/README.md)。截图有独立采集包绑定，最终包仍 fail-closed；不是正式发布证据。后续 E/F/I/J 工程继续推进。

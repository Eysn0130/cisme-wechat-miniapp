# WeChat DevTools checkpoint — package `ea24ffe9…`

Capture date: 2026-08-16. This historical checkpoint is bound only to mini-program source SHA-256 `ea24ffe9d7acfbd83da313c8e21557555475b815fd7faba6510e0845f26bebb0` (128 files, 13 routes, 1,287,869 bytes, global WXSS 7,844 bytes). Source changed after the authorization-row/CTA-group finding, so none of this evidence is inherited by current `ecd59318…`. The runtime is WeChat DevTools Stable 2.01.2510290, base library 2.32.3 and `touristappid`; it is not evidence of the tracked AppID, a development QR or a physical device.

## Capture-snapshot evidence

- `visual/current-run/native/devtools-package-ea24ffe9-compile-0-problems-2026-08-16.jpg`: current source after compile; Problems reports zero detected issues.
- `visual/current-run/native/devtools-package-ea24ffe9-console-0-errors-warnings-2-2026-08-16.jpg`: Console with the Errors-only level selected and no error row. The debugger reports two tool/base-library warnings; this is not warning-clean evidence.
- `visual/current-run/native/devtools-package-ea24ffe9-account-unchecked-button-baseline-2026-08-16.jpg`: full DevTools window for Account with the explicit local-only legal fixture, agreement unchecked and primary CTA disabled. It is a layout checkpoint, not a same-viewport native original.

No current-hash Network trace is recorded. `networkFailures` remains unknown and the strict gate fails closed. Prior filtered empty Network screenshots are historical bounded views, not structured current-source evidence.

## Scope and verdict

This checkpoint follows the Account checkbox-shell/button alignment fix, Submit busy-indicator correction, care-mutation leave protection, navigation serialization and appeal-conflict fail-closed fixes. Target typecheck, 27 flow/native tests and package budget passed before capture; a post-capture Page harness then executed the double-tap/failure-unlock and appeal-conflict state machines, bringing the current unit gate to 57/57 without changing mini-program source hash. The checked/enabled Account state still requires the user to personally accept the local agreement fixture; it has not been captured here.

This is not a release pass. The 13-route state matrix, same-state Web/reference/native/comparison triplets, authenticated flows, raw Network evidence and real iOS/Android proof are incomplete. `final result: blocked`.

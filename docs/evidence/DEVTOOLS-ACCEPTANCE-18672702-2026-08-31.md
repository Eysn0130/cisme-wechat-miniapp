# WeChat DevTools checkpoint — 18672702 — 2026-08-31

## Bound source

- Mini-program source SHA-256: `186727029d8c2aa352fb8eb8fff9ea70e6cb654b09c9e0c79a9d8f6839469d2c`.
- Package gate: 129 files / 13 routes / 1,299,124 bytes; global WXSS 8,117 bytes.
- Tool: WeChat DevTools RC 2.02.2608031, base library 2.32.3, interface-test-account project, iPhone X simulator at 50%.
- This checkpoint is diagnostic evidence only. It is not a controlled development QR, experience upload, review submission, release, or real-device pass.

## Current-hash evidence

The current manifest binds exactly five 1250×733 JPEG full-window captures:

1. final compile: Problems `0`;
2. settled Debugger: Errors `0`, Warnings `0`;
3. Progress `submitted` local visual fixture;
4. Progress `needs_changes` local visual fixture at default scroll;
5. Progress `needs_changes` local visual fixture at scroll bottom.

The three Progress frames are explicit disposable-database visual fixtures. They verify page hierarchy, reason-card layout, fixed-action clearance and the corrected button label baseline in the simulator; they do not prove that media upload, submission, review, appeal or points awarding occurred. No media picker was opened, no consent or legal checkbox was selected, and no review API action was executed. After capture, the temporary review case/actions were removed and the authoritative submission was restored to `draft`, version 1.

## Retested interaction

- Progress Back now returns to the stored Task source only when the preceding stack page is the same Task ID; otherwise it redirects to that exact Task and has a public Community fallback.
- Matching Task→Progress stack return and single-page Progress→exact Task redirect were exercised in DevTools. Unit tests additionally lock unrelated-stack, redirect failure and rapid double-click behavior.
- “按补件要求修改” entered Submit for the same submission ID.
- Progress bottom actions use a 50px flex row and `.action-label` at 16/20px with the shared 1px optical baseline adjustment. The current default and scroll-bottom frames show no button-label or safe-bottom collision.

## Remaining acceptance gaps

- All 13 route matrices remain `matrixComplete:false`; Progress has only three diagnostic fixture states and the other 12 routes require current-hash recapture.
- There is no exact page-frame, same-fixture Web/native comparison, checked/enabled Account frame, structured Network trace, iOS/Android real-device evidence, production WeChat identity, real media flow, or complete submit→review→supplement/appeal→points trail.
- Formal privacy/terms text and version identifiers are unsigned. Target AppID ownership/project role, legal domains, upload key and allowlisted CI egress are unproved.

`final result: blocked`

# CISME 服务交付验收矩阵（2026-09-23）

依据附件第五部分的 14 项反例和现行 PRD §8.2、A13、AFS-01/02。各列独立记录；`有源码`只表示可定位到实现，`隔离通过`只表示合成测试范围。当前 PR22 仍是 Draft，本文不批准合并、生产部署、真实资金动作或微信发布。每次源码变化须以当前 `current-source-acceptance.json` 的包哈希重新核验原生证据。

| 附件验收项 | 源码与隔离证据 | 原生交互 | 生产装配与真实渠道 | 待闭环 |
| --- | --- | --- | --- | --- |
| 申请受理 | `AftersaleService.request` 在同一事务写案件、客服关联、系统消息和 outbox；空说明无理由与幂等路径有隔离测试 | 底部面板有明确提交与重入读取；当前哈希全状态未验 | 未装配 | 正式账号、客服离线及超时重入现场验收 |
| 会话关联 | 支持原会话、消息序列及可靠事件；隔离路径已覆盖 | 完整长会话、重连、翻页未按当前哈希验收 | 未装配 | 故障注入下的消息补投与设备恢复 |
| 地址指引 | 逐案版本快照、权限、幂等及审计有源码与隔离测试 | 管理聊天与用户复制/寄回入口存在；当前哈希未实测 | 未装配；固定地址不作为受理前提 | 正式收件资料、管理员真账号与真实物流 |
| 地址变更 | 版本历史保留；已有登记运单时拒绝静默改写；无指引时不进入待寄状态 | 未实测 | 未装配 | 用户已按旧有效地址寄出但尚未登记运单的人工协助路径、延误升级规则 |
| 退款事实 | 申请、渠道意图、回调、质检、库存独立事实；重复和乱序隔离测试已有范围 | 当前哈希未实测 | 真实微信支付、退款、库存和对账未验 | 正式退款与异常重开范围批准及渠道对账 |
| 合理例外 | 丢件、漏发、拦截退回、无法寄回有授权免寄回动作和依据；隔离测试有范围 | 当前哈希未实测 | 未装配 | 多商品/部分数量/部分金额、历史优惠及运费分摊仍缺真实闭环 |
| 费率规则 | 测试环境有全局/单独提议、独立复核、未来生效和快照；生产生效仍关闭 | 管理成员页有入口，未做当前哈希全状态验收 | 正式费率与资格期限未确认，未执行真实调整 | 摒弃工程枚举作为经营事实；装配正式配置并验证并发、历史退款 |
| 界面边界 | 面板复用原客服状态管理；账号切换与待办迟到响应有单元测试 | 小屏、大字、键盘、长文、弱网与返回恢复未完整实测 | 未装配 | 微信开发者工具和授权 iOS/Android 矩阵 |
| 隐私请求 | 受理、队列与合成身份字段级执行存在；本人下载/权限/重试隔离测试有范围 | 当前哈希未实测 | 正式导出、删除和注销执行器不存在 | 全主体图、他人排除、私有交付、受限保留、缓存/对象清理 |
| 保留和备份 | 合成字段政策和局部客服保留守卫存在 | 不适用 | 正式分类保存政策、目标恢复及防再现未验 | 生产分类规则、对象图与备份恢复后抑制重新出现 |
| 协议发布 | 完整消费者正文候选、成对发布校验和配套清单结构已提交 | 原生页读取当前活动 API；新正文未发布，故未验 | 主体名称经经营者确认；公开联系渠道、保存清单和第三方事实未完 | 事实核对、正式隐私执行、微信后台隐私指引一致后才发布 |
| 告警 | 独立 systemd 探测器及 HTTPS webhook 重试/去重有离线测试；站内待办计数按权限过滤 | 管理待办当前哈希未实测 | 未安装探测器，未有真实外部接收与送达 | 生产安装、故障注入与真实接收确认 |
| 测试隔离 | 隐私分页写入改用独立身份；单独运行及全套集成已通过 | 不适用 | 合成测试，不代表真实账号 | PR CI 与更大顺序变化继续确认 |
| 发布证据 | 当前包哈希见 `docs/evidence/visual/current-source-acceptance.json`；本地类型、单元与集成有通过记录 | 当前 40 路由矩阵、DevTools、iOS 和 Android 均未标通过 | PR22 未合并；main CI、正确 production 备份/迁移/回滚未执行 | 准确 PR CI、独立源码审阅、main push CI、目标实例恢复与上线后核验 |

## 当前实测边界

- 原分支 `codex/fulfillment-lifecycle-20260922`、PR22 保持原位，普通 push；main 未合并。准确 CI 以 GitHub 的最新 HEAD run 为准，不能用本地或旧提交运行替代。
- `docs/evidence/release-preparation-20260921/audit/api-surface.json` 已按当前服务端源码重建：251 个方法已映射，13 维完整审阅通过数仍为 0。映射是审阅索引，不是安全验收。
- `npm run test:integration` 于本轮本机隔离环境通过 851 项；最近的管理待办定向集成用例单独通过。管理待办当前原生包静态检查和类型检查通过，但 DevTools 工具授权尚待用户完成，未取得当前包截图。
- 生产服务器、现有数据库与 COS 未在本轮修改；任何真实退款、历史资料不可逆删除或真实成员费率调整仍需分别界定具体操作范围。

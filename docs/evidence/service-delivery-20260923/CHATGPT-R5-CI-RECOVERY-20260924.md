# R5 CI 恢复与接续（2026-09-24）

## 本轮事实

原仓库/分支/PR22不变。初始 Codex 基线为 b4db47dc105d7f449a9822c49aed619384fce5c0。
ChatGPT R5 应用修复为 b5f69b3ed0e0abe2fa291ab013fe5a96e49a294d，交接为 d2420682729a04b41f9908094da25d2853141682。
本轮恢复时实际核对 d242068 的 CI 35968111539：1279 项单测通过，隔离集成883通过/2失败。因此上一批不能认定为完整验证通过。

## 已修失败，保留反例

修复提交 e0d3f58b02485be6b493035dc7384471e245b69d。

1. applyProfileErasure 首次收到请求时，数据库已依次转到 executing，但函数内的 request.status 仍为 received。因此可返回 completed 而记录仍 executing，下一次无变化重放会错误撤销刚生成的副本。本次在成功状态转换后同步本地状态，使首次调用完整提交结果、完成时间与事件。不放宽身份、法定保全或撤销条件。
2. 新增恢复反例使用的 RestoredOldPrivateProfile 超出数据库微信号约束，本次改为合法夹具 RestoredOldProfile。没有删除反例或放宽 schema。
3. 强化原集成测试：第一次返回后立即断言数据库 completed、SELF_PROFILE_ERASED 与完成时间；无变化重放不得增加版本，不得重复成功事件，不得撤销后来生成的副本。
4. 原有保全解除后删除与进行中导出的并发、恢复已删资料、注销后新导出、用户主动重新填写同一昵称等反例继续保留。

这不是新的注销功能或新审批流程。此次无依赖、路由、原生包或数据库迁移变更。临时来源取证/补丁工作流在修复提交内移除，没有留常驻扩展系统。

## 验证口径

本机隔离源码副本的类型检查与 diff check 已通过；不是用户 Mac 工作树。
本机 Node 22 与仓库 Node 24 不同，权威全套结果应以这次提交的原 CI、Node24、真实一次性 PostgreSQL 为准。本文提交时尚未取得最终 CI 结论，必须查询该最新文档提交对应的 run，不能套用 b4db/d242 的旧结果。最终结果写入原 PR 回执，不通过反复添加文档提交制造检查循环。

原生包仍为 896db226379f646a5b8b642a8334705bb1f397156b911194b5eb93a04f1bd3b3。旧 ed0c3ce… 截图仅代表旧包部分模拟器状态；本次不冒称新的 DevTools/真机验证。

## 剩余任务按已冻结范围推进

继续完整读取 docs/CISME-R5-FROZEN-RELEASE-CLOSURE-20260924.md。

- 已有正式数据投影导出、可选资料删除、最小注销和关联客服到期任务均已存在；不要重新宣称只有 synthetic-only，也不要重新建系统。
- 明确导出未覆盖/超限素材在原请求与管理端的实际补交方式。其他删除范围的实际执行、保全及结果告知必须闭环，不能只有“转客服”。不能把 fullMemberExportImplemented 或完成状态改真来代替交付。
- 已全额退款但未发货/无签收的旧 paid 订单，关联客服清理仍可能一直判定未结束；新无关订单又可能延长所有历史消息。先用原退款权威终态和原关联关系复现，再修；不试删生产历史。
- 当前 launchFulfillmentPolicy 明确 shippingCents=0、大陆快递可达地区包邮。因此非零原运费比例逻辑是历史/其他合同兼容问题，不能把它无条件扩大成所有首发订单的必现错误；退货寄件运费与原配送运费仍须区分、消费原承诺。
- 关闭账号后在途订单、售后、已有余额/佣金不能只“保留行”，要能沿原受限入口和原管理端办理完成。不得要求重建会员或新客服。
- 核对已公布商业规则/政策配置是否在正确 production 装配；用户已确认经营主体、唯一客服渠道和36个月基线，不再重复索要。
- 原生验收集中在当前包的购买/取消支付/退款/客服往返、真实键盘、小屏/大字、按钮loading/disabled及卡片文本，不扩大设计体系。原生低信息量错误、同身份草稿保留及登录切换清理一起验证。
- PR 当前必要检查与核心源码条件满足后正常合并，再核对合并后的 main 自身CI和构建；真实生产目标、备份隔离恢复、迁移、API+Worker、COS/删除抑制、回滚及告警实收按原授权执行。不可把备案未过当作停止这些准备工作的理由，也不得绕过微信备案/代码审核/发布条件。

本回执不宣称 main 已合并或生产已部署。已推送、CI通过、原生已测、生产已部署、公众已发布必须分别报告。

# Contact sheets

These files are navigation aids and never replace the raw frames:

- `healthy-routes.png`: all 27 authenticated/public route-health-checked current frames in `app.json` order.
- `before-8e843a6-routes.png`: all 27 immutable baseline frames.
- `after-key-routes.png`: representative current U1 routes.
- `before-after-key-routes.png`: representative old/current comparison.
- `interaction-keyframes.png`: index of the 6 timed interaction sequences.

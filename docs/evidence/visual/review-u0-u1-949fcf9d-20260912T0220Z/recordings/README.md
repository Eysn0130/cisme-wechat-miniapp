# Recordings

No video recording exists in this local run. Six current-source timed PNG keyframe sequences are preserved in `../keyframes/` with steps, expected/actual results and exact source binding in `../interaction-evidence.json`.

Physical iOS/Android recording remains required for keyboard, safe-area, font scaling, weak-network and platform behavior. Future video must record device/OS/WeChat versions, synthetic role/state, source revision and timeline without exposing account secrets or real user data.

# 2026-09-08 两端精修核验

本轮按用户 Web 批注同步微信原生：邀请页卡片换行、编号基线、底部短文案、固定尺寸信任图标；商城留白、价格数字；商品轮播及手动停止；积分页短文案和图标。

## 已实际验证

- 邀请：Web 窄屏空态与生成态；原生首次生成真实分享链接，重新编译并再次进入仍读取 B94AC9D7。未向任何联系人发送。原生截图 `invite-reopened-native.jpg`。
- 商品：原生原图和两张补充护理图片共三张，自动切换；手动横滑后暂停入口消失，超过轮播周期仍固定；箭头切换后也停止自动播放。最终箭头使用 Phosphor regular 白色资产，避免缩放后过淡。截图 `product-final-native.jpg`。Web 同样通过横滑、箭头、停止自动播放检查。
- 积分：零余额、空账本、精简提示及浏览商品入口；截图 `points-native-after.jpg`。
- 原生商品与邀请最终检查开发者工具 Errors: 0；本地服务通过已授权的本地不校验域名设置访问。正式项目配置仍校验域名。

## 校验

Web 构建、29 个保护运行时文件校验通过。原生与后端类型检查、85 项单元测试通过。分享后端变更在隔离数据库完成全套 31 项集成测试。原生包 14 路由、136 文件、1,670,797 字节，小于内部 1,800,000 字节预算。

核验时小程序源 SHA256：`9f43eac0eeed464057c4ad297b343c3e25cd5d3483cdc058cb6843eaeed7c320`。

## 数据与边界

Web 使用演示数据，原生使用已认证接口数据；商品名称、价格和用户头像以各自数据源为准。编号仅为分享引用，不承诺手工输入兑换。生成记录不代表发送成功。本轮截图为局部交互证据，未冒充完整 14 路由状态矩阵或 iOS/Android 真机验收；正式发布验收仍 blocked。

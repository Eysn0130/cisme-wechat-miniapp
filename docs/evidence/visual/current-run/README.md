# Current visual evidence run — 2026-09-01

This directory preserves originals, normalized review copies and combined comparisons. Intermediate images remain for the `found → fixed → retested` trail; files named `final` were only the last verdict inputs for their named historical capture snapshot. Where a package hash is absent, the image is explicitly a pre-checkpoint diagnostic and cannot support any current-worktree acceptance.

## Current CLI checkpoint and historical DevTools checkpoints

- Current package: 129 files / 13 routes / 1,301,625 bytes; global WXSS 8,117 bytes; SHA-256 `405ae15c319e5d4c92d157edebf4c0a57f5ae0cd72741a9d0b27b0b97856efde`. Current WeChat DevTools RC 2.02.2608031 evidence includes Problems 0 and, after the final compile/settle window, Debugger Errors 0 / Warnings 0. The current non-QR diagnostics bind compile/console plus Community brand-editorial at iPhone X 50%. A temporary public-HTTPS Quick Tunnel and the `CISME 异地真机调试` condition generated an internet-reachable short-lived QR with LAN mode off. An authorized iPhone 17 Pro Max / iOS 26.5.2 connected over 4G, but DevTools warned that first QR targeted Android and the tunnel received no business request. That mismatched session is transport diagnostic only. A corrected iOS QR was generated afterward and still awaits rescan.
- The current checkpoint is recorded in `docs/evidence/DEVTOOLS-ACCEPTANCE-405ae15c-2026-09-01.md`. The manifest references exactly four current-hash artifacts, including the mismatched-profile iPhone transport window; both device platforms and every route remain `blocked`.
- Package `22352d6b…` is the immediately preceding historical same-LAN device-debug checkpoint. Its three diagnostics cannot be rebound after the controlled public-HTTPS boundary was added.
- Package `857110dd…` is the historical Account compact-height checkpoint. It fixed and retested the 50px primary/secondary actions and unchecked legal-entry baseline, but those screenshots cannot be rebound to current source.
- Package `18672702…` is the historical Progress checkpoint. It fixed and retested source-aware Task return plus the 50px/16-20px/+1px bottom action baseline, but was superseded by later Account and true-device source changes.
- Package `6857791b…` is the immediately preceding historical 36-artifact all-route diagnostic/local-API checkpoint. It was superseded by the Progress source-aware Back and bottom action-label fix and cannot be inherited by `18672702…`.
- On 2026-08-30 a shortcut was interpreted by the RC as Preview and generated a short-lived development QR for the logged-in interface test account. It was not scanned, distributed, promoted to trial, submitted or released. The QR-bearing screenshot was deleted; the non-QR interruption context is retained outside the acceptance manifest. This event is not release evidence.
- Package `66165d22…` is a historical intermediate after the first Account Back suppression change. Its remaining ten JPEGs are deliberately excluded from `current-source-acceptance.json`: that implementation used one-shot suppression and Profile's concurrent protected requests could still reopen Account. The misleading Profile intermediate was deleted rather than rebound; none of the remaining `66165d22` files is current acceptance evidence.
- Package `85b8077f…` is historical after the Account Back automatic-auth suppression and concurrent protected-request recovery fix. Its five diagnostic artifacts remain bound to the historical checkpoint and are not inherited.
- Package `703377ee…` is historical after the protocol-button centering and false-affordance correction. Its 664×1434 iPhone X 100% diagnostic includes outer slivers, is not an exact page-frame and cannot be rebound to current source.
- Package `ecd59318…` and a byte-identical `3f508c21…` Account raw file are historical. The copied/relabelled raw frame cannot be rebound to current source.
- Package `ea24ffe9…` is the immediately preceding button baseline and is historical after the measured authorization-row/CTA-group correction.
- Package `59855054…` is the immediately preceding DevTools checkpoint and is historical after button, navigation and appeal-state fixes.
- Package `d68a641e…` is the immediately preceding Account/DevTools checkpoint and is historical after button/legal changes.
- Package `64561172…` retains the historical 13-module public/no-session traversal, Account/Community originals/comparisons, public Community 41-request partition and missing/back diagnostics. Every `64561172` file below is historical and cannot support current-source acceptance.
- Latest exact Home/Records visual checkpoint remains historical `5e2b2801f0bf4f31f4bd3e915bf7bab6e11d3d5966d2f2c5f0f7d44272888e7b` (1,213,536 bytes).
- Prior `9e536f82…` DevTools evidence is documented in `docs/evidence/DEVTOOLS-ACCEPTANCE-9e536f82-2026-08-15.md`. It retains a compile, 13-module lazy-load traversal and a 41-entry public-community HAR with no status 0/4xx/5xx. It is historical and not inherited by current source.
- `native/devtools-package-5e2b2801-compile-0-project-errors-2026-08-15.jpg` binds that package to WeChat DevTools Stable 2.01.2510290 on `pages/account/index`: project error 0 / Problems 0; two Chromium/base-library warnings were separately classified. `native/devtools-package-5e2b2801-console-cleared-0-error-0-warning-2026-08-15.png` records the same source after classification and console clear. `native/devtools-package-5e2b2801-network-16-local-no-visible-failures-2026-08-15.png` records a fresh 16-request local window with no visible failure; it is not a HAR.
- Historical `5e2b2801…` same-state native originals are `native/home-planned-5e2b2801-375x812-2x.png` and `native/records-planned-5e2b2801-375x812-2x.png`, both DevTools 750×1624 output for the iPhone X 375×812 logical viewport. Their 1× review copies are preserved alongside them.
- Capture SHA-256: Home Web `ff795af9a95c35d6bbfa2e65917482e636f5acbcabffeb1eb4107816876d9960`, Home native 2× `e61d8a6ddb527c0eff7135e28c6d19d43f53988bdf87cbbe1132da4c53354676`; Records Web `519a26e417437a01e6abff8dabf457e619ec62b5abaeb373882b58ef139238c6`, Records native 2× `b9c7600449985f44a04c4a878e0316eb2ee12f74453bb2cae10cb4bab2684f83`.
- Home binding comparison is `comparisons/home-planned-5e2b2801-same-coordinate-web-vs-native-y44-375x734.png`: both sides use `x=0,y=44,w=375,h=734`. The full comparison is `comparisons/home-planned-5e2b2801-web-vs-native-375x812.png`. This route/state has no visible product P0/P1; remaining wordmark/serif weight and glass opacity differences are P2. The asymmetric `home-planned-5e2b2801-shared-content-web-vs-native-375x734.png` is retained as a crop-method finding because Web y44/native y0 creates a false 44px displacement.
- Records `5e2b2801…` comparisons are findings, not passes. The full comparison is `comparisons/records-planned-5e2b2801-web-vs-native-375x812.png`; the asymmetric shared-content file mixes safe-area and fixed-TabBar coordinate systems. Current source fixes the measured safe-area/card-density and typography drift while preserving production-correct “0 条记录”; recapture is required before assigning a local pass.
- Historical `64561172…` contains a complete 13-module public/no-session traversal but not a complete 13-route state traversal. Protected modules were checked only at the no-session client precheck and Account recovery boundary; required-auth requests were not dispatched. Its raw page-frame and combined visual inputs remain limited to Account/Community and cannot be inherited by current source.

## Historical DevTools checkpoint for package `f2ec3156…`

- Snapshot package: 127 files / 13 routes / 1,206,759 bytes; global WXSS 5,553 bytes; SHA-256 `f2ec3156173837537868e5b875660cf858ee69eb107791dc8277fd571e1d8fbb`.
- `native/devtools-package-f2ec3156-final-compile-0-errors-3-system-warnings-2026-08-15.jpg` binds that historical hash to WeChat DevTools Stable 2.01.2510290 on `pages/account/index`: project Errors 0, Problems 0 and three separately classified tool/base-library warnings. `native/devtools-package-f2ec3156-final-console-cleared-0-errors-0-warnings-2026-08-15.jpg` records the same snapshot after classification and console clear.
- `native/devtools-f2ec3156-route-*.jpg` preserves one full-window screenshot for every exercised route; task has both authoritative `available` and `claimed` states. These images prove only that historical snapshot's lazy loading/default-state rendering.
- Historical real local API trail: task `415ec79c-fe8f-4132-a9a2-8a77209e45a4` → claim `39747391-7306-4ef4-83e4-e88ee8314124` → server submission `62be9f64-3c8f-4394-a28d-7b8c67e5871b`, initially `draft` version 1. Returning from submit displayed “继续完成投稿” for the same ID. The database was then changed to an explicitly labeled `submitted` visual fixture only to open progress; the sequential integration suite later reset the disposable database. No media E2E is claimed.
- `native/devtools-f2ec3156-profile-scroll-bottom-2026-08-15.jpg` and `native/devtools-f2ec3156-profile-repeat-tab-back-to-top-2026-08-15.jpg` record the profile bottom reserve and repeated-current-Tab return to top.
- `native/devtools-package-f2ec3156-network-16-local-requests-no-visible-failures-2026-08-15.jpg` records a fresh post-compile Network window with 16 local resource requests and no visible failed entry. It is deliberately not described as an all-flow HAR or as proof of the earlier API interaction history.
- The runtime URL still identifies `appid=touristappid`; tracked config ownership and execution under the AppID-shaped value remain unverified.

### Superseded package `812a483f…`

- `native/devtools-package-812a483f-account-entry-compile-0-errors-3-system-warnings-2026-08-15.jpg`, `native/account-package-812a483f-375x812-2x-2026-08-15.png` and their combined diagnostics remain as the finding trail that exposed cross-route typography and target-size drift. They are not current-source acceptance.

## Historical exact planned fixture capture (package hash not recorded)

- Route/state: `home`, authenticated member `佳静`, care cycle `planned`, selected protocol step `01`, copy aligned to the frozen prototype.
- Web source URL: `/?qa=1&member=verified&fixture=care-planned&qa_clock=2026-08-14T12%3A00%3A00%2B08%3A00#/home`, device preset `Narrow 375 × 812`.
- Web original: `web/home-planned-fixture-375x812-exact-2026-08-15.png`, 375×812 CSS pixels, browser DPR 1, captured from the unscaled `.device-screen` at 375×812.
- Historical native original: `native/home-planned-fixture-375x812-2x-final-2026-08-15.png`, WeChat DevTools iPhone X logical viewport 375×812, 750×1624 output pixels (2×), captured before package-hash binding existed.
- Historical normalization: `native/home-planned-fixture-375x812-css-px-final-2026-08-15.png`, mechanically downsampled to 375×812.
- Historical full viewport comparison: `comparisons/home-planned-fixture-web-vs-native-375x812-final-2026-08-15.png`.
- Historical shared content crop: `comparisons/home-planned-fixture-content-web-vs-native-375x734-final-2026-08-15.png`; both sides use the identical crop `x=0, y=44, width=375, height=734`.

The Web prototype intentionally renders an iOS status bar, Dynamic Island, rounded device mask and home indicator. DevTools' exported page-frame screenshot omits those operating-system overlays. They were not copied or fabricated into the native image. The full comparison therefore verifies the 375×812 app viewport and business state, while the shared content crop is the binding homepage layout comparison. Real-device platform-chrome acceptance remains blocked.

## Finding trail

1. Initial native capture had a planned cycle but different member/copy/step selection and a vertically compressed console. It could not be treated as same-state evidence.
2. The native view now reads the member display name, matches planned copy and selects `00 done / 01 active` like the source.
3. Homepage narrow-layout measurements were rebuilt from the prototype's active 375px overrides; CTA, protocol rail, note and 70px liquid tab bar were recaptured and compared together.
4. The clean compile captured after selector cleanup reported DevTools project `Errors: 0, Warnings: 0`. During the prior live pass, the community array spread exposed a missing `@babel/runtime/helpers/toConsumableArray` helper; it was replaced with a runtime-safe `concat`, then the console was cleared and a full compile returned 0/0. Local login subsequently adds the expected development-only domain-validation/automatic-reload warnings; those are not represented as production clean-network evidence.
5. After the task/submit/progress fixes and full CLI gate run, DevTools was compiled again. The compile produced zero project errors and three identifiable platform/base-library warnings (`SharedArrayBuffer` deprecation and WeChat base-library/HarmonyOS guidance). Those were classified, then the console was cleared to 0 errors/0 warnings and the Problems panel remained 0. Evidence: `native/devtools-final-compile-console-cleared-0-errors-0-warnings-2026-08-15.png`. A fresh Network panel was empty with no failed entries in `native/devtools-final-network-empty-after-compile-2026-08-15.png`; that narrow window is not claimed as historical all-flow network proof.

The corresponding earlier capture-snapshot full DevTools window is preserved as `native/devtools-clean-compile-0-errors-0-warnings-selector-final-2026-08-15.jpg`; the accessibility-tree reading is the source of that explicit 0/0 count. Historical `64561172…` remains the latest 13-module public/no-session checkpoint and historical raw-frame checkpoint for Account/Community only; `4584a4c2…` and `9e536f82…` are also historical, `5e2b2801…` remains the exact Home/Records historical visual checkpoint, and `f2ec3156…` retains older authenticated interaction evidence.

Historical capture-snapshot verdict: no P0/P1 was visible in that one route/state/crop. The package hash was not recorded and source has since changed, so this is superseded diagnostic evidence, not a current or whole-product visual pass.

## Historical exact unlogged account fixture (package hash not recorded)

- Route/state: `account`, no session, required agreement unchecked, primary action disabled.
- Web original: `web/account-unlogged-375x812-exact-2026-08-15.png`, frozen Web `Narrow 375 × 812`, DPR 1.
- Historical native original: `native/account-unlogged-375x812-2x-final-2026-08-15.png`, DevTools iPhone X logical 375×812, original 750×1624 output.
- Historical normalization: `native/account-unlogged-375x812-css-px-final-2026-08-15.png`.
- Historical OS-normalized app-content comparison: `comparisons/account-unlogged-shared-content-web-vs-native-375x734-final-2026-08-15.png`. The Web side removes its simulated 44px system overlay; the DevTools page-frame side begins at y=0 because that export omits the system overlay. It is not current or real platform-chrome evidence.

The first native capture showed an undersized identity lens, collapsed typography, ignored `<br>` behavior, compressed card geometry and a weak CTA hierarchy. The native page was rebuilt with explicit WXML line blocks, source-measured type/spacing/card/button geometry and a soft-square native checkbox treatment. A later package `812a483f…` comparison still exposed a systemic utility/community text-size and hit-target mismatch. Current source applies the cross-route typography/hit-target remediation and fixes critical Account/all-route action labels with explicit UI font, zero vertical padding, no flex shrink, flex centering and a 1px optical baseline adjustment. Historical `8b593ce9…` exposed native disabled-button opacity; historical `64561172…` raw/combined Account evidence located that fix but is not inherited. The `ea24ffe9…` raw diagnostic then showed that equal-height authorization rows pushed the whole CTA group down; later source restored measured row density. Historical `6857791b…` confirmed the unchecked main/secondary buttons and protocol entry were locally centered, removed the dead identity caret, and retained all-route diagnostic/local-API coverage. Historical `18672702…` recaptured Progress after its Back/button correction. Historical `857110dd…` recaptured Account after its compact-height correction: both actions were fully visible and centered on iPhone X 812px, while the checkbox remained untouched. Current `405ae15c…` adds the tightly scoped public-HTTPS physical-device development boundary and recaptures Community; checked Account, long-copy, large-text, the other 12 current-hash routes, full matrices and connected real-device states remain open.

## Records planned trail

- Web truth: `web/records-planned-fixture-375x812-exact-2026-08-15.png`, authenticated `佳静`, planned cycle, 0/4 milestones and no records.
- Native finding input: `native/records-planned-fixture-375x812-2x-r1-2026-08-15.png`.
- Combined finding: `comparisons/records-planned-fixture-shared-content-web-vs-native-375x734-r1-2026-08-15.png`.
- Historical r4 capture input: `native/records-planned-fixture-375x812-2x-r4-2026-08-15.png`, original 750×1624 DevTools output; package hash was not recorded.
- Historical r4 full comparison: `comparisons/records-planned-fixture-web-vs-native-375x812-r4-2026-08-15.png`.
- Historical r4 app-content comparison: `comparisons/records-planned-fixture-shared-content-web-vs-native-375x734-r4-2026-08-15.png`.

The r1 native page used a different information architecture: day-first authoritative ledger, line-track milestones and a full pending-milestone list. It did not match the frozen hero, streak, four-box cycle ledger, lifecycle card and recent-record structure, so it was a P1 finding rather than a pass. The r4 native page restored the frozen structure while retaining authoritative API data and working cycle actions. Historical `5e2b2801…` then found mixed safe-area/crop coordinates, taller card rhythm and the deliberate production-correct removal of the fake “查看全部” affordance. Current source fixes the measurable geometry/type drift but has no recapture, so Records remains `found`, not passed; real-device chrome and Android typography also remain blocked.

## Community default trail

- Frozen Web input: `web/community-default-375x812-exact-2026-08-15.png`.
- Initial native input/comparison: `native/community-default-375x812-2x-r1-2026-08-15.png` and `comparisons/community-default-web-vs-native-375x812-r1-2026-08-15.png`.
- Latest native input/comparison: `native/community-default-375x812-2x-r4-2026-08-15.png` and `comparisons/community-default-web-vs-native-375x812-r4-2026-08-15.png`.

The r1 native header, hero and feed were vertically compressed and the two-column assets were not 4:5. In r4, the frozen header/hero/grid start lines, 238px hero, separate author/action layers and 4:5 feed geometry were restored. Historical `8b593ce9…` then exposed a rendering P1: the hero title wrapped as `9 + 1`, orphaning “式”. Historical `64561172…` raw and combined frames showed a balanced two-line title after the 46rpx/49%/44rpx/1.36 mapping; that result is not inherited by current source. This is still not a same-state pass: the frozen input contains public UGC and recommendation/latest affordances, while production permits only manually curated brand editorial. The native page therefore uses non-interactive “精选 / 人工审核” context and does not fake UGC engagement or sorting. A formal pass needs a current-hash frozen brand-editorial reference or fully approved UGC state with matching copy/data, plus the remaining state/device evidence.

## Task claim and resume trail

- Frozen available source: `web/task-available-375x812-exact-2026-08-15.png`.
- Latest available geometry input/comparison: `native/task-available-375x812-2x-r3-2026-08-15.png` and `comparisons/task-available-web-vs-native-375x812-r3-2026-08-15.png`.
- Real claimed native evidence: `native/task-claimed-375x812-2x-r4-2026-08-15.png`, original 750×1624 from DevTools.

The available comparison aligns the header/hero boundaries, step-card rhythm and fixed CTA closely enough for local geometry review, but it is not a same-copy pass: the frozen prototype presents an active points reward while the local configuration intentionally disables unsigned points rules and displays “审核闭环 / 体验”. The claim action was executed in DevTools against the local API. At capture time, task `66666666-6666-4666-8666-666666666666` → claim `f546cb0d-7d6c-471e-bb88-b6df267daac8` → submission `10b85e42-838d-40ce-8569-e9c342819601`, status `draft`, version `1`, `license_payload={}`. The disposable database was later reset and no longer contains that chain. Returning from submit exposed a stale state and drove the `onShow` refresh fix; the image proves the capture-time interaction, not current DB contents or current-source visual acceptance.

## Submit claimed diagnostic trail

- Frozen Web full-browser source: `web/submit-claimed-browser-1280x720-narrow-r3-2026-08-15.jpg`, in-app Browser `Narrow 375 × 812` preset displayed inside the 1280×720 review shell.
- Auditable normalized Web crop: `web/submit-claimed-375x812-normalized-r3-2026-08-15.png`, mechanical crop `x=504, y=64, width=272, height=592` from the scaled device screen, then resized to 375×812. It is deliberately named `normalized`, not `exact`.
- Native original: `native/submit-draft-explicit-consent-off-375x812-2x-r1-2026-08-15.png`, DevTools iPhone X 375×812 logical viewport, 750×1624 output.
- Diagnostic combined input: `comparisons/submit-web-vs-native-375x812-diagnostic-r1-2026-08-15.png`.
- Post-fix native original and diagnostic: `native/submit-draft-explicit-consent-off-375x812-2x-r2-2026-08-15.png` and `comparisons/submit-web-vs-native-375x812-diagnostic-r2-2026-08-15.png`.

The two sides share the claimed/draft route but not the same fixture or production copy. The frozen Web still specifies 3–9 originals, four legacy confirmations and a 24-hour/points promise; the native production page uses one representative original, an explicit benefit disclosure, necessary storage/review purposes defaulted off, and an independent optional public-display purpose defaulted off. Those business corrections must not be reverted merely to match a screenshot. The r1 combined input revealed a native rendering defect: WeChat's default `textarea` height overrode the visual rhythm and pushed section 03 below the first viewport. The WXSS now sets an explicit 108rpx height; r2 confirms that section 03 and the fixed CTA return to the intended first-screen rhythm. This fixes the rendering defect but does not turn mismatched business fixtures into an exact pass. No simulator chooser, local object-storage adapter or static screenshot is treated as real camera/album upload evidence.

## Progress submitted and needs-changes trail

- Frozen Web submitted full browser and normalized crop: `web/progress-submitted-browser-1280x720-narrow-r1-2026-08-15.jpg` and `web/progress-submitted-375x812-normalized-r1-2026-08-15.png`. The crop uses the same documented `x=504, y=64, width=272, height=592` scaled-device normalization as submit and is not labeled exact.
- Submitted native progression: `native/progress-submitted-375x812-2x-r1-2026-08-15.png` through `r3`; latest combined diagnostic is `comparisons/progress-submitted-web-vs-native-375x812-diagnostic-r3-2026-08-15.png`.
- Needs-changes native evidence: `native/progress-needs-changes-375x812-2x-r2-top-2026-08-15.png` and `native/progress-needs-changes-375x812-2x-r2-bottom-2026-08-15.png`.

The first submitted capture exposed a real rendering defect rather than a subjective spacing preference: the controller spread `title` and `subtitle` into page data while WXML read `statusTitle` and `statusSubtitle`, leaving the central state message blank. The explicit mapping was fixed and recaptured. The first combined comparison also showed a 60–70 CSS-pixel vertical compression across the status summary and timeline; r3 restores the frozen hierarchy and pacing while retaining production-correct copy. The Web source still promises a 24-hour result and points delivery, whereas the native page intentionally makes neither unsigned promise and labels the last step “结果处理”. Therefore r3 is a geometry diagnostic, not a same-copy pass.

For needs-changes, the local database was deliberately transitioned after the real claim evidence into a documented visual review fixture: version `3`, review reason code `EVIDENCE_INCOMPLETE`, public summary rendered by the service. The top and bottom captures prove the reason, same submission ID, supplement CTA, single page scroll owner and separate source/community returns. Clicking “按补件要求修改” reopened submit with the same ID; clicking “返回投稿来源” returned to the task with “继续完成投稿”, without the old progress↔task loop. This fixture does not claim that real camera/album media traversed the flow.

## Prepared Web references awaiting native recapture

- `web/community-default-375x812-exact-2026-08-15.png`: verified-member default editorial community state; exact 375×812 crop from the preserved 600×1000 browser capture.
- `web/profile-planned-375x812-exact-2026-08-15.png`: verified `佳静` / planned profile source. Its 1,280-point source fixture is not yet equivalent to the current native 0-point fixture, so it has no native verdict.
- `web/task-available-375x812-exact-2026-08-15.png`: valid invitation / available task source. Native geometry has been captured, but the unsigned-points configuration intentionally produces different reward copy, so an exact same-copy verdict remains blocked.

The remaining progress/points/post/shop/product/settings source states have auditable 1280×720 preparation captures and DOM state records, but are intentionally not labeled exact 375×812 evidence. Submit now also has a documented scaled-device normalization for diagnosis only; those pages still require unscaled, same-copy device-screen capture before formal comparison.

These are source-side preparations only. They have no `passed` verdict until the corresponding native route/state is captured and combined. Their uncropped 600×1000 browser frames are preserved where a browser clip returned a blank GPU layer, so the measured x=112/y=94/375×812 crop remains auditable rather than reconstructed.

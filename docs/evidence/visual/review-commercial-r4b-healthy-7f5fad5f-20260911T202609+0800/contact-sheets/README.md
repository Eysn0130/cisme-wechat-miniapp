# Contact sheets

`healthy-routes.png` is a navigation aid generated from authenticated, route-health-checked raw frames. Tile order follows `app.json`; use `routes.csv` for route metadata. It never replaces raw evidence.

# Contact sheets

`default-routes.png` is a navigation aid generated from the stable raw frames. Alphabetical tile order follows raw filenames; use `routes.csv` for route metadata. It never replaces raw evidence.

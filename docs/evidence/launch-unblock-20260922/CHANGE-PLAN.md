# 待批准的具体云端准备变更

状态更新：用户明确暂不执行 A；B 的 staging 候选部署已获本轮明确授权，实施以全新隔离数据库/对象命名空间、旧部署可恢复为边界。以下 A/B 为上一轮提案历史，不再把其待批准状态当作施工阻断，也不必为了复用历史提案安装 Docker。正式 production 切换、真实资金、续费扣费及微信提审/发布未授权。

## A. staging 证书续期通道

- 唯一目标：上海 `lhins-ei4hz4fi`（150.158.39.74）。
- 拟新增一条实例防火墙规则：来源 `0.0.0.0/0`，TCP 80，允许，备注 `CISME staging ACME HTTP-01`。
- 已核验 Nginx 80 上只有 `/.well-known/acme-challenge/` 的静态验证目录，其他路径 404。443、SSH、生产实例、DNS 不变。
- 影响：向公网开放 80；为 ACME 多地验证所需，属于扩大网络可达面，必须取得对应批准。
- 批准后先复查 Nginx host/location 与无业务代理、保存该规则唯一 ID，再执行 certbot 对该域名的 staging dry-run；不强制生产续签，不降低证书校验。若失败，保留脱敏错误并移除本轮新增规则恢复原状态，不能宣称自动续期通过。

## B. 独立云端演练环境（与 A 分开批准）

- 唯一宿主仍为上述 staging 实例；已有 `/opt/cisme/current`、systemd 服务、`cisme_staging`、`cisme_test`、现存对象存储与生产资源保持不动。
- 该宿主目前没有 Docker。拟从 Docker 官方 Ubuntu 仓库安装 Docker Engine，不添加用户到 docker 组、不开放 daemon TCP 端口；这会新增 root 级守护进程，需对应批准。
- 独立目录 `/opt/cisme/rehearsals/ae31437652e2-20260922`，若存在则拒绝复用；候选源码 HEAD 为 `ae31437652e2fe3fbb24e7ac493d3b60ad747d27`，manifest hash 见 README。
- 使用新的随机 run ID、容器标签与临时 DB/S3 凭证；PostgreSQL 18.4、SeaweedFS 4.29 仅绑定 loopback 动态端口，无现有目录或卷挂载，无生产凭证，无微信支付网络调用。资源总预算不超过 1 CPU、1.5 GiB；宿主空间当前约 39 GiB。
- 在 Linux 安装候选包锁定依赖；执行 hash gate、72 迁移首次/重复演练、health/鉴权 smoke、合成数据备份与恢复；原生设备接入和域名路由不包含在本次变更内。
- 只允许销毁本轮 run ID 且 ownership 校验匹配的新容器、卷和合成数据；出错停止，不触碰现存数据。保留脱敏结果与制品清单；既有部署无切换，因此无需回退旧数据库。
- 不购买资源、不设置收费监控或续费、不发真实资金、不扩大云账号 IAM 权限、不上传体验版/提审/正式发布。

## C. 其余准备的决策边界

- 公众/商户控制台的工具站点策略限制不能通过 Chrome 原生、其他工具或会话凭证绕过。仍需平台提供的允许通道，或由用户给出最少脱敏事实：AppID 与商户关联/授权状态、合法域名、正式隐私版本及联系方式；不要提供密钥或 Cookie。
- 本机代理与 utun4 的例外设置尚未变更；先识别代理软件/规则与组织要求，另行提出精确配置变更，不能以关掉 TLS 校验替代修复。
- SSH 22 收紧与资源续费另需确认实际团队需求/金额，不捆绑进 A/B。

# CISME 下一阶段增量施工入口

请继续实际施工，不启动 Goal 目标模式。完整长期要求仍是 `docs/CHATGPT-GITHUB-NEXT-PROMPT.md` 全文；必须连同 AGENTS、当前 PRD、最新 main、本文和 `docs/CISME-COMMERCIAL-RUNTIME-DELIVERY-2026-09-21.md` 阅读。本文只更新已施工事实和顺序，不删除原来的商业、原生、性能、安全、平台及运营退出条件。

先查 PR #6 是否合并、准确 main SHA、当前 CI、并行 PR 与工作树。若 PR #6 未合并，沿原任务分支处理当前 HEAD 的检查和 review，不另写一套；若已合并，从实时 main 开新小批 codex/ 分支。不要把原 main 42e9b44 或报告中的中间候选当最新主线。PR4、PR5 不重复应用。

本批已经实现三页核心/runtime 渐进读取与独立重试、页面读取所有权、本人历史查询关门可读及最小字段、退款数量 unknown 与 zero 区分；532 单测是这一阶段本地记录，不是下一阶段免测票。REL-01 正式 Provider、全37页验收和跨卸载资金命令恢复没有完成。当前原生包 8fca4a19907fb7d5d57463c367d63aa2f407d0c490d3b32351d599599e4b91d5 的 manifest blocked；仅作接续比对，以实时包为准。

## 按可独立交付的小批执行

第一批先复现 MONEY-RECOVERY-01：资金写请求服务端可能成功而响应丢失；编辑金额/理由、离页卸载、进程被杀后不能静默换键。沿已有 care-command-state、请求协调器和服务端幂等查原结果设计按主体隔离、版本化的命令恢复记录。只存必要操作编号/规范化载荷/对象版本，不存 token、密钥、收款参数或聊天内容；确认未知前不允许制造新金融操作。覆盖请求未发出、已提交丢响应、已完成、未知、冲突、重启、A→B→A、注销清理和保留恢复事实的差别。前端取消不等于支付未发生；门关闭仍可读历史，但命令执行及确认参数继续独立授权。先 RED 后修复，再安全反向与完整回归。不要为这个阶段启用真实支付。

第二批从当前脚本输出的37页逐页记录调用链，优先订单列表/护理记录/管理队列和客服。缺直接 pageRead 标记并不证明没有取消，先跟踪包装器、共享消费者、定时器和返回恢复。已有内容可保留为只读，401/403及换号必须清敏感事实；不要机械拆开结算商品/地址/报价一致性，也不要取消写请求后显示未执行。每个已修页交真实字段和正反向回归，不只断言接口成功。

第三批推进正式能力 gap：前后端能力版本、适用微信 APIv3 直连/服务商模式、正式适配器、验签/解密/乱序重复回调、主动查单/对账、部分退款/库存竞争/履约签收/护理资格来源。复用现有模块，缺平台证据仍完成可测试代码与明确接线清单，保持出站关闭。真实商户、正式交易验证额度/范围和凭据注入需既有门禁，不能要求在聊天给私钥。购物车等后续范围不扩入。

同步扩展 API 主体×对象×字段×动作授权分母、隐私全量目录/导出/删除 hold 与注销下游、运营升级/客服投诉规则。缺业务政策标责任人和最小证据，不伪造审批或把全部代码任务退回用户。

## 原生、G0 与部署

Chat 只能验证实际可用工具；不能声称操作了 Mac。Mac Codex 接续时沿 `/Users/mini/CISME` 唯一目录，先检查未提交工作、分支、origin 和最新 Actions，保全改动后只做合法 ff 同步，不 reset/clean/强推或另建永久工程。微信工具 project 参数为 `/Users/mini/CISME/apps/miniprogram`。用户在操作时不抢占；沿已有窗口与真实项目列表恢复，不把路径提示当理由复制项目。

逐页当前包原生截图、按钮外框及内部文字、字号间距长金额、键盘/胶囊/安全区、导航与弱网/换号/返回、减少动效及低端 Android 必须实际验证。缺设备证据保持 NOT EXECUTED，不能用网页截图、模板编译或 mock PASS 代替。按 PRD §13 与 NFR-MEASUREMENT 先固化 G0 口径，P95 API≤500ms/页面≤2s，失败超时计分母，真实HTTPS与app.inject分开，禁止未经确认生产施压。

重新核对历史 staging 和现用资源，不触碰未知部署或 .env。准备 immutable release manifest：正式发布组、exact代码/包/配置/迁移、域名、测试/真机/平台证据、回滚及负责人。未批准 manifest 只能候选准备，不能标 releaseReady。rulesets/main保护/environments 需实时读取；无管理接口时交精确方案并记未配置，不能把普通 verify 或 skipped 当发布许可。

## 验证与交付

每批实现、复现前后证据、类型/单元/隔离库集成/构建/契约/包/路由/依赖审计/diff检查后，PR准确HEAD检查和review、main并行差异处理、正常合并、独立main CI。原生变化运行 scripts/update-current-source-manifest.ts，历史图/哈希原样保留。禁止为“过关”放宽预算、删负面测试、替换假支付或安装重框架。

最后交代码数量、缺陷/延期/阻塞账本、37页逐角色状态与原生证据、同环境性能前后分母、实际采用资源及许可、准确PR/main/Actions/包SHA、Mac同步/部署/体验版/审核分别状态、回滚与下一动作。本批验收完成不等于全部商业版完成；缺外部证据也不停止独立代码工作。没有证据就明示未验证，releaseReady仍由本次正式发布组全部退出条件共同决定。

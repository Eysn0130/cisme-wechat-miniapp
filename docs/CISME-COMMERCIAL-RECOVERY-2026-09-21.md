# CISME PR7：持久原命令恢复（源码阶段）

## 起点与本阶段范围

从 main `f4adf37a6ac52ca07f5e40eea614651c3a805f2a` / 已合并 PR6 接续。进入本轮时 PR7 HEAD `c841bf51604b577a697643738aaef80290937993` 只有临时工具链准备，没有业务修复。后续受审 HEAD/CI/main 由 PR7 评论绑定，不用旧绿色结果替代。

本阶段实现 MONEY-RECOVERY-01 的退款申请、结算意向、测试购物权益转换/撤销、普通/已核验渠道订单取消。它们只保存本地恢复提示，不成为第二套业务事实；权威结果仍来自既有领域表及幂等记录。真实支付/退款/转账、生产、公开UGC及真实隐私执行均没有开启。

## 原缺陷与修复

原页面的键存在 Page.data；修改金额/理由会清空键，卸载/杀进程会丢失键。六项真实 Page 合成诊断（退款/结算/转换 × 编辑金额/模块重启）在旧源码全部失败，新源码全部通过；不是以 mock 接口成功代替实际页面逻辑。

- `commerce-command-store.ts`：按已认证稳定会员、API/Cloud目标、订单或共同佣金作用域保存原键/规范化载荷。最多16条、64000 UTF-16序列化单元，不缓存支付参数、token/OpenID/服务端响应。发送前同步保存并读取校验；容量、损坏、未知版本、写入/读回失败均阻止新发送，不静默清除或替换。
- `commerce-command-recovery.ts`：写前通过现有已鉴权profile获取稳定归属；重试先查原回执，再只用原键原载荷执行。修改金额/理由或切换消耗同一资产的命令不能静默新建。会话修订、页世代、目标环境防止 A→B→A 旧授权继续发送。已发送请求不因离页解释成未执行；迟到回执只清对应主体原键，不回填新页。
- 退出时清除本地自由文本和金额载荷，但保留最小未知命令提示；后续可查询权威回执，未核实且载荷已清除时转人工核对，不凭新表单重建。存储损坏导致清理失败时不阻止清除登录身份，也不宣称损坏字节已被抹除。
- 两页有真实恢复入口；权威已记录仍须用户核对历史后明确确认，不在后台静默清除后允许再次提交。申请已记录不等于退款、转账已到账。
- 新增 `GET /v1/me/commerce/command-receipts/{kind}`：原键经header而非URL发送，最小白名单结果，`Cache-Control:no-store`；身份/会员状态/本人对象/取消主体/原记录版本均保留。查询与出站能力分开，`not_observed` 不证明没有执行，仍可能有在途事务。现有写入鉴权、门禁、业务版本、唯一键和事务未降低。

对应 PRD §8.2、§10.3.1、§11.2–11.3、WX-PAY-MAKE-01、AFS-03；同一请求的身份、键和载荷是恢复条件，不是客户端资金授权。

## 验证与边界

本地临时 Linux、原锁文件、Node24.18.0：基线538单元通过；本阶段628单元通过，包括新增90项（Page6、存储25、恢复服务57、请求/会话2）。原生Page测试不是WXML渲染或设备测试。类型、构建、OpenAPI契约（225个/v1方法、含health227方法）、包、37路由、设计结构及diff检查通过。首批准确 HEAD `4d0145ea6110382321553b2d9a77d85244a8a9c4` 的正常 CI `35591953041` 已独立通过，原始工件确认628单元/659集成（新增58项集成）及两类根审计0。之后的扩展不能继承此成绩；本地没有PostgreSQL，最终集成以最新HEAD/合并main各自工件为准。

没有迁移或新依赖；全局WXSS仍8137/8192 bytes。当前原生包由 `scripts/update-current-source-manifest.ts` 更新；历史b7a绑定保留，当前依旧blocked、releaseReady=false。没有Mac同步、WeChat编译/截图/双平台或真实HTTPS证据。源码变更后的准确包以当前manifest和最终CI工件为准。

## 没有被夸大为完成的边界

业务明确拒绝、权限撤销、订单冲突且查不到原结果时，安全地保留待核对提示；它不是“失败已确认”或自动放行另一命令的依据。已退出并擦除载荷、系统/用户清除本地数据、换设备、存储损坏都不能被宣传为自动完整恢复；本人历史和运营人工核对仍必要。跨设备原命令提示发现及经批准的人工解除协议未在此阶段实现。当前原生包尚需核验恢复文案、触控、键盘与后台/杀进程恢复。

本阶段不完成订单物流/售后案件三轴、正式provider接线、全部37页生命周期或全接口对象权限；上述仍沿原完整商业提示词推进，不能说只剩Codex。购物车等已批准后续范围不扩入。G0签字、平台、商户、隐私/保留政策和发布manifest仍由对应责任人提供真实证据，代码不能代签。

## 回滚

通过新revert撤回本阶段运行代码，重新计算包并复跑旧回归；无数据库迁移。含未知命令的本地版本1记录不得在回滚中静默删除：旧客户端不识别恢复提示，故资金动作应保持关闭直到原操作人工核对或恢复支持版本。回滚并不自动撤销已提交的申请或渠道事实。不要回退权限/幂等/事务/PR4/PR5/PR6修复。

## 接续扩展：订单列表、记录与认证分母

- `PERF-12-B`：orders / management-orders 的 GET 改用自己的订阅 owner，隐藏/卸载取消自己的消费者；权限预读也受该页的世代/身份保护。刷新保留只读快照而不闪空，但导航须重新核实；401/403/404清除，分页去重且拒绝旧游标响应。管理页 paid 不再显示内部字符串。15项行为回归，原源码10失败/5通过，修复后15通过；未证明原生滚动位置/FPS。
- `CARE-READ-WRITE-OWNER`：records 隐藏取消GET，不取消周期写入；无后台重刷/跨账号回填/隐藏后nextTick复活弹层。普通失败可保留标记的历史事实，但周期动作保持关闭直至重新核实；403清除。原生确认属于原读世代，换号/返回/刷新后不发送旧动作。周期成功必须核对相同周期、目标phase及更高version；写回执丢失但权威事实已变更，不说“执行失败”。隐藏写回执不能清除另一可见页的全局忙态，定时器按Page隔离。14项旧源码诊断全部失败后通过；另2项二次审阅诊断RED→GREEN，共16项。
- `SEC-AUTH-DENOMINATOR`：实际挂载Fastify，当前194个session必需方法各测无身份/无效身份，共388请求，均401且未访问数据库；另1项固定分母验证。32个显式无session和1个可选session接口不被伪称同样覆盖，支付回调/私有媒体等仍有各自签名/对象要求。匿名拒绝不是完整主体×对象×字段×动作覆盖。
- `PRIVACY-CATALOG`：新增仅在显式测试库运行的迁移后完整public BASE TABLE/字段/FK结构清单，输出CI工件`privacy-schema-inventory.json`。不读取业务行/列默认值/URL/凭据；所有字段处置、hold和导出授权标待审，不能把它叫全量个人数据导出。新的本机恢复提示已纳入隐私数据目录，保留规则未替责任人签字。

Gitleaks中间失败实际命中 `commerce-command-store.test.ts` 的固定合成操作编号。例外限定generic-api-key规则、唯一文件路径及精确字面量AND匹配；原正常扫描仍全量运行，未排除目录、改成警告或忽略失败。临时工具链/传输/扫描诊断从候选tree清退，历史提交保留追溯。628首批正常CI已证明新配置通过；最终新SHA必须重跑。

本地完整扩展验证与最终包/代码数量以PR7收口评论和交付回执为准。`tests/unit/miniprogram-flow-safety.test.ts` 的旧字面量随更严格的生命周期/按钮条件更新，继续检查存活+身份+版本及按钮禁用一致性；不是删除反向断言。
